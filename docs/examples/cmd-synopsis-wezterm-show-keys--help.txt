Show key assignments

Usage: wezterm show-keys [OPTIONS]

Options:
      --ktav
          Show the keys as ktav config statements, ready to paste into the
          `keys`/`key_tables` sections of your config file
      --key-table <KEY_TABLE>
          In ktav mode, show only the named key table
  -h, --help
          Print help
