# Checkpoint — 2026-07-28 17:10 [hebrew-rtl-visual-swap-bug]

## Session summary
Прямое продолжение прошлого чекпоинта (`2026-07-28-0957.md`), после которого коммит `e504c422a` закрыл первый пакет фиксов (Cascadia Mono, ClusterResolver sort/wrap/mirroring bugs) — он уже запушен. Всё, что описано ниже, произошло ПОСЛЕ этого коммита и НЕ закоммичено.

1. **Смена архитектуры рендера иврита**: по явной просьбе пользователя полностью отказались от Unicode Bidi Algorithm (`BidiContext::resolve_paragraph`/`reordered_runs`) в пользу простого, предсказуемого алгоритма в `crates/onlyterm-surface/src/cellcluster.rs` (`CellCluster::make_cluster_with_bidi` → `reorder_hebrew_phrases`): строка всегда растёт слева направо от колонки 0 (как обычная LTR), ищутся максимальные "ивритские фразы" (буквы, склеенные пробелами/гершем/гершаим/мак(assign)афом, только если иврит реально продолжается после склейки), и ТОЛЬКО порядок ЯЧЕЕК внутри такой фразы переворачивается. Всё остальное (цифры, скобки, кавычки, тире, латиница/кириллица) никогда не двигается и не зеркалируется. Заодно это устранило целый класс багов, связанных с harfbuzz RTL-shaping (шейпинг теперь всегда идёт как LTR, `cluster.direction` больше нигде не выставляется в RightToLeft).
2. **Заменили шрифт для иврита**: Noto Sans Hebrew (проприоционный) → Cascadia Mono (SIL OFL, генuinely-моноширинный для ивритских согласных). Пользователь явно попросил НЕ тащить GPL-шрифты (Miriam Mono CLM и другие Culmus-шрифты отклонены). Cascadia Mono не покрывает кантилляцию и часть огласовок — они рендерятся как отсутствующие (0 ширины), а не notdef-коробкой (отдельный фикс в `rustybuzz.rs`: если буква нашлась в шрифте, а её огласовка нет — буква сохраняется, огласовка просто не рисуется, вместо того чтобы вся графема схлопывалась в notdef).
3. **Добавили `onlyterm_bidi::mirror_char`** (сгенерирован из `bidi/data/BidiMirroring.txt`, был в репе, но не использовался) для зеркалирования скобок/кавычек (UAX#9 L4) — но после перехода на "простой алгоритм" (см. п.1) мирроринг оказался НЕ НУЖЕН для скобок, которые никогда не двигаются с места (сам вызывающий код в `cellcluster.rs` его больше не использует; функция осталась в `onlyterm-bidi` как общая утилита).
4. **Новый инструмент для быстрой проверки рендера без пересборки всего GUI**: `crates/onlyterm-font/examples/render_line.rs` — шейпит строку через настоящий пайплайн (`Line::cluster` → `RustybuzzShaper`) и растеризует в PNG. Поддерживает `--text-file` (важно: передача иврита через аргумент командной строки в Git Bash иногда искажается/обрезается — использовать только `--text-file`). НО: этот инструмент — самописная реализация позиционирования (копирует логику `screen_line.rs` вручную), а НЕ вызов настоящего рендер-пайплайна GUI. Это оказалось ключевой слепой зоной (см. п.6).
5. **Фикс переноса строк (wrap)**: физическая строка при переносе видит только свои ячейки, поэтому ивритская фраза, разрезанная переносом, могла развернуться некорректно. Добавили `Line::cluster_with_wrap_context(bidi_hint, is_wrap_continuation)`, прокинули `is_wrap_continuation`/`continues_next` через `pane.rs`/`screen_line.rs`/кэш-ключи (`LineQuadCacheKey`, `LineToEleShapeCacheKey` — оба получили новое поле). Если ивритская фраза касается края строки, который может быть неполным (перенос), она НЕ переворачивается.
6. **Фикс Alt/Ctrl на не-английской раскладке** (`crates/window/src/os/windows/window.rs`, функция `key()`): Windows транслировал физическую клавишу через ТЕКУЩУЮ раскладку (V → «м» на русской) до вызова keybinding-lookup, поэтому `Alt+V` не матчился. Теперь при зажатом Ctrl или обычном Alt (не AltGr — используется уже существующее разделение `Modifiers::ALT` vs `Modifiers::RIGHT_ALT`) используется `phys_code.map(|p| p.to_key_code())` — физический код клавиши независимо от раскладки.
7. **ГЛАВНАЯ НЕРЕШЁННАЯ ПРОБЛЕМА** (task #173): пользователь показал скриншот (файл `C:\Users\Computer\Desktop\pc\hebrew_wisdom.txt`, секция "Вариант 4"), где в КАЖДОЙ строке вида `"<иврит>" / "<русский>"` (и с другими типами скобок: «», „", '', (), [], {}) реальный GUI показывает **русскую половину ПЕРВОЙ, ивритскую ВТОРОЙ** — то есть две половины строки перепутаны местами, хотя в файле иврит идёт первым. Внутри каждой половины текст читается корректно (иврит правильно развёрнут, русский не тронут) — переставлены местами именно ЦЕЛИКОМ ДВЕ ЧАСТИ. Я (основная сессия) потратил очень много времени и НЕ СМОГ воспроизвести это через `render_line`/юнит-тесты (`hebrew_phrase_reverses_in_place_without_touching_neighbors`, `diag_quoted_hebrew_then_russian_char_by_char` — оба ВСЕГДА дают правильный, файловый порядок). Проверил и отверг гипотезы: (a) config `bidi_direction` — подтверждено `AutoLeftToRight` и в дефолте, и в обоих `wezterm.lua` пользователя (`~/.config/wezterm/wezterm.lua` и `~/Desktop/wezterm.lua`); (b) `ParagraphDirectionHint::AutoLeftToRight.direction()` — чисто статический матч, всегда `LeftToRight`, контент не влияет; (c) перенос строк — не может объяснить свап на КОРОТКИХ строках типа `(איזהו עשיר) (кто богат?)`; (d) grapheme-based (`Line::from_text`) vs посимвольная (`set_cell`) сборка строки — дают ИДЕНТИЧНЫЙ (правильный) результат.
8. **По явной просьбе пользователя делегировал расследование агенту `@ox`** (`Agent` tool, `subagent_type: "ox"`, launched as async/background agent, agentId `aa6dfcd37de2d2383` — НЕ упоминать этот ID пользователю напрямую, использовать `SendMessage` с `to: agentId` для продолжения). Дал ему исчерпывающий контекст (все отвергнутые гипотезы, все файлы, точный воспроизводящий пример).
9. **Агент `@ox` уже что-то нашёл и начал править код**, судя по diff'ам, показанным системой при получении `/checkpoint` (файлы изменились "не мной напрямую" — это агент): добавил в `crates/onlyterm-gui/src/termwindow/render/mod.rs` НОВОЕ ПОЛЕ `LineToElementShape::first_visual_cell_idx: usize` с очень говорящим комментарием: *"Column this cluster is actually drawn at... This is NOT `cluster.first_cell_idx`: a Hebrew phrase gets reordered within the line, so its clusters' logical cell indices run backwards while they are still painted one after another from the start of the line... anything else drawn per-cluster (backgrounds, underlines) has to use the same counter or it lands underneath a different piece of the line."* — это ПОХОЖЕ НА НАСТОЯЩУЮ ПРИЧИНУ БАГА: где-то в рендере (вероятно, отрисовка ФОНА/подчёркивания под кластером, НЕ сама отрисовка глифов) использовался `cluster.first_cell_idx` напрямую как если бы кластеры шли в монотонном логическом порядке — а после моего алгоритма (п.1) это не так (ивритская фраза физически рисуется в одном месте, но её `first_cell_idx` — это исходный, "дореверсивный" номер ячейки, который может быть БОЛЬШЕ чем у кластера, нарисованного ПОСЛЕ неё). Агент также тронул `crates/term/src/screen.rs` и `crates/term/src/terminalstate/mod.rs` (не проверял зачем — возможно нашёл смежную проблему), плюс создал скриншоты/кропы (`shot1-5.png`, `crop_*.png`) и лог (`bidi_debug.log`), `v4.txt` — явно воспроизвёл баг вживую и итеративно отлаживал.
10. Задача #174 (Alt+V) реализована и, насколько я тестировал компиляцией, должна работать, но живого теста от пользователя ещё не было (был отвлечён на задачу #173 сразу после билда).
11. **`/checkpoint` был вызван пользователем ПРЯМО ПОСЕРЕДИНЕ работы агента `@ox`** — агент, скорее всего, ВСЁ ЕЩЁ РАБОТАЕТ в фоне (или только что закончил — были показаны diff'ы файлов, которые он уже успел изменить). Это самое важное, что нужно проверить в первую очередь при возобновлении: доделать/дождаться агента, посмотреть его финальный отчёт, проверить его правки компилируются и не сломали остальное (все 53 юнит-теста, которые я написал), и т.д.

## Active goal
Нет — `/goal` не устанавливался.

## TaskList
### in_progress
- #173 Найти и исправить баг с разворотом скобок/кавычек в реальном GUI (не воспроизводится в render_line) — делегирована агенту `@ox`, который похоже уже нашёл причину (`first_visual_cell_idx` в `render/mod.rs`) и правит код
- #174 Alt/Ctrl+клавиша не работают на русской раскладке (Alt+V) — код написан и компилируется, живой тест от пользователя не получен

## Decisions
- Полностью заменили Unicode Bidi Algorithm на простой "переворачивай только ивритские буквы на месте" алгоритм — по явной просьбе пользователя, в обмен на предсказуемость (курсор/selection всегда по типографской колонке) вместо строгого соответствия UAX#9.
- НЕ тащить GPL-шрифты (Miriam Mono CLM и другие Culmus) — выбрали Cascadia Mono (OFL, Microsoft) как компромисс: моноширинные согласные, но не покрывает кантилляцию/часть огласовок.
- Убрали мирроринг скобок из `cellcluster.rs` (оставили `mirror_char` как общую утилиту в `onlyterm-bidi`, но не вызываем) — он оказался не нужен, раз скобки никогда не двигаются с места в новом алгоритме.
- При зависании на баге со свапом сегментов — по прямой просьбе пользователя делегировал расследование агенту `@ox` вместо продолжения соло (пользователь явно сказал "спроси помощи у @ox").

## Open questions
- **Чем закончил/закончит агент `@ox`?** Нужно проверить его результат (SendMessage к agentId `aa6dfcd37de2d2383`, если он ещё не завершился, или дождаться уведомления) — это САМОЕ ПЕРВОЕ действие при возобновлении сессии.
- Если правка агента верна — нужно пересобрать релиз, перезапустить терминал (в `C:\Users\Computer\Desktop\pc`, через `--cwd`), и попросить пользователя проверить и "Вариант 4", и Alt+V.
- Не коммичено НИЧЕГО с момента `e504c422a` — большой пакет изменений (весь новый алгоритм, шрифт, wrap-фикс, Alt/Ctrl-фикс, плюс что бы ни доделал `@ox`) ждёт явной команды "коммит".
- Разбросанные файлы от агента (`shot1-5.png`, `crop_*.png`, `bidi_debug.log`, `v4.txt`) в корне репо — не проверял, надо ли их удалить перед коммитом (это диагностический мусор, не должен попасть в git).

## Repo state
```
 M crates/bidi/generate/src/main.rs
 M crates/bidi/src/lib.rs
 M crates/term/src/screen.rs
 M crates/term/src/terminalstate/mod.rs
 M crates/onlyterm-font/examples/render_line.rs
 M crates/onlyterm-font/src/shaper/rustybuzz.rs
 M crates/onlyterm-gui/src/termwindow/render/mod.rs
 M crates/onlyterm-gui/src/termwindow/render/pane.rs
 M crates/onlyterm-gui/src/termwindow/render/screen_line.rs
 M crates/onlyterm-gui/src/termwindow/render/tab_bar.rs
 M crates/onlyterm-surface/src/cellcluster.rs
 M crates/onlyterm-surface/src/line/line.rs
 M crates/window/src/os/windows/window.rs
?? .claude/
?? OnlyTerm-Setup.exe
?? bidi_debug.log
?? crates/bidi/src/bidi_mirroring.rs
?? crop_l2_head.png
?? crop_l2_tail.png
?? crop_line2.png
?? crop_line5.png
?? docs/upstream-research/issues_chunk_00
?? docs/upstream-research/issues_chunk_01
?? docs/upstream-research/open-bug-issues-by-comments.txt
?? docs/upstream-research/open-prs-titles.txt
?? shot1.png
?? shot2.png
?? shot3.png
?? shot4.png
?? shot5.png
?? v4.txt
```
```
e504c422a onlyterm-font, onlyterm-surface, onlyterm-gui: fix Hebrew bidi/shaping bugs, swap to Cascadia Mono, kill panes on GPU context loss
d27c07f70 config, onlyterm-font: revert default font to JetBrains Mono, harden Hebrew shaping
3f589a694 assets: darken the icon's arrow for better contrast
c1ce86710 gui: keep text selection visible after mouseup, add default Ctrl+V binding
32b54e94c config: Lucida Console default font, bundle Noto Sans Hebrew, bidi + scrollbar on by default
```
Все показанные коммиты уже запушены. Всё, что выше в `git status`, не закоммичено.
