# Checkpoint — 2026-08-05 07:34

## Session summary

OnlyTerm — Windows-ориентированный форк onlyterm со снятым скриптовым слоем
(конфиг ktav, ни Lua, ни rhai). Основной объём этой части сессии — двухэтапная
массовая уборка (вычистка `clippy` по всему воркспейсу + распил всех крупных
файлов по правилу «один экспорт на файл») — **полностью завершён и
закоммичен** до коммита `e358f2104`. Обе фазы, финальная цепочка
(changelog/чекпоинт/коммит md) и ревью агентом `@oh` (#367) пройдены.

Ревью `@oh` нашло семь реальных, не связанных с самим распилом дефектов.
Я проверил самые серьёзные лично (не поверил отчёту), подтвердил все, и по
запросу пользователя перешёл к их исправлению — тем же методом: бриф →
`crush run` в фоне → моя проверка диффа → коммит.

**Текущее состояние дерева нечистое и требует внимания на старте следующей
сессии.** Причина: пока сессия `crush` (`fix-config-unsafe`, задача #376)
работала над `crates/config/src/version.rs`, я параллельно запустил
`cargo fmt --all` по всему воркспейсу (задача #378, механическая
переформатировка) — не дождавшись, пока сессия `crush` завершится. Это была
гонка записи: `cargo fmt` тронул тот же файл, что правила сессия `crush`.
По счастью коллизии не случилось — сессия успела внести правку раньше, чем я
запустил `fmt`, и `fmt` лишь переформатировал уже применённое исправление
(расширенный `unsafe`-блок в `version.rs`, охватывающий `mem::zeroed()`,
`libc::uname` и оба `CStr::from_ptr`) — но я это заметил и остановился,
дальше не форсировал. **Сессия `fix-config-unsafe` только что завершилась
(уведомление получено в момент написания этого чекпоинта), её результат ещё
не прочитан и не проверен.**

Сейчас в рабочем дереве **90 изменённых файлов, ни один не закоммичен**:
смесь чисто форматных правок (`cargo fmt --all`, затронувших почти все файлы
из недавнего распила — естественно, поскольку распил вставлял новые
`mod`/`use`-строки, а `cargo fmt --all -- --check` после распила показывал
143 расхождения в 66 файлах) и содержательной правки `unsafe`-блока в
`config/src/version.rs` + `config/src/config_impl.rs`. Эти два слоя изменений
в диффе НЕ разделены и НЕ проверены построчно — следующий шаг обязателен:
прочитать отчёт сессии `fix-config-unsafe` (`.crush/stdin/fix-config-unsafe.out`),
проверить именно содержательную правку (сборка под
`x86_64-unknown-linux-gnu`, тесты, clippy), затем решить, коммитить ли
formatting-правки и unsafe-фикс одним коммитом или раздельно.

Параллельно с этим ещё три сессии `crush` были подготовлены, но **не
запущены** (только briefs записаны в `.crush/stdin/`): `fix-termwiz-bugs.prompt`
(#374 scroll_region, #375 Numpad9, #377 unused import — все три в одном
брифе, так как один крейт) и задача #373 (`onlyterm-input-types`,
дублирующийся код `57436` для `VolumeDown`/`MediaPrevTrack`, бриф ещё не
написан). Задача #379 (мелкая уборка `#[allow]`) тоже не начата — планировал
делать её напрямую, без `crush`.

**Свод находок ревью `@oh` (#367)**, все заведены как задачи:
- #373 `kitty_function_code`: код `57436` дублируется в ДВУХ независимых
  match-блоках (`KeyCode` и `PhysKeyCode`), не в одном как было заведено
  изначально; правильное значение — `57438` (уточнено сверкой со
  спецификацией kitty keyboard protocol);
- #374 `termwiz::scroll_region` использует `left` вместо `top` — подтверждено;
- #375 `termwiz::Numpad9` кодируется как PageDown (`\x1b[6`) вместо PageUp
  (`\x1b[5`) — подтверждено;
- #376 `config` не собирается ни под один unix-таргет — подтверждено, тот же
  класс дефекта, что уже реально ломал `pty/src/unix.rs` в этой сессии;
  **правка внесена сессией `crush`, результат ещё не проверен мной**;
- #377 `termwiz`: неиспользуемый импорт `MouseButtons` на не-Windows
  таргетах, внесено моим же распилом #351 — подтверждено;
- #378 `cargo fmt --all -- --check` падал на 143 расхождениях в 66 файлах —
  подтверждено, **`cargo fmt --all` уже применён**, но диффа ещё не проверен
  и не закоммичен;
- #379 мелкая уборка (`#[allow(dead_code)]` на живом поле,
  задвоенный lint) — подтверждено, не начато.

Также уточнена собственная задача #368: было ошибочно записано «42/44»,
реальные цифры — «41 passed, 2 failed, всего 43 при изолированной сборке»,
исправлено в описании задачи.

## Active goal

Явного `/goal` нет. Действует Stop-hook `babygoal`-условие (повторяется в
фидбеке после каждого ответа): «реализуй задачи с помощью /crush, между
тасками делай коммиты. После завершения всей работы сделай /checkpoint,
обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть
проверит всю работу». Это условие формально выполнено (объяснено
пользователю и подтверждено вопросом через AskUserQuestion: пользователь
выбрал «чинить найденные баги сейчас» — то есть текущая работа над
#373–#379 идёт по его прямому запросу, не по повторному срабатыванию
исходного babygoal-условия).

## TaskList

### in_progress
- #373 kitty_function_code: дублирующийся код 57436 (бриф не написан)
- #374 termwiz: scroll_region left/top (в общем брифе fix-termwiz-bugs, не запущен)
- #375 termwiz: Numpad9 PageDown/PageUp (в общем брифе fix-termwiz-bugs, не запущен)
- #376 config: unsafe-блок в version.rs (правка сессией crush внесена, НЕ ПРОВЕРЕНА мной)
- #377 termwiz: неиспользуемый импорт MouseButtons (в общем брифе fix-termwiz-bugs, не запущен)

### pending
- #368 onlyterm-surface: два snapshot-теста падают только при -p (цифры уточнены)
- #369 crush-fallback state (persistent — do not complete): sh — armed
- #370 Многие символы рисуются квадратиком (tofu)
- #371 Шрифт тонкий на WebGPU (нет субпиксельного сглаживания в WGSL)
- #372 Падение при возврате на WebGPU — НЕ ТРОГАТЬ без согласования пользователя
- #378 cargo fmt gate не проходил на HEAD (fmt уже применён, дифф не проверен/не закоммичен)
- #379 Мелкая уборка: лишний #[allow(dead_code)], задвоенный lint

### recently completed
- #367 Финал: ревью всей работы агентом @oh
- #366 Финал: чекпоинт и коммит всех md
- #365 Финал: обновить changelog
- #337–#351 (все 14 задач распила)
- #352–#364 (все задачи вычистки clippy)

## Decisions

- На вопрос пользователя о судьбе находок `@oh` выбран вариант «чинить
  найденные баги сейчас», тем же методом (`/crush` + моя проверка каждого
  диффа), с явным исключением #372 (не трогать без отдельного согласования).
- Механические/тривиальные фиксы (#378 fmt, #379 allow-уборка) решено делать
  напрямую, без `/crush` — там нечего делегировать, весь дифф виден с одного
  взгляда. Содержательные фиксы (#373–#377) — через `/crush`, поскольку
  требуют проверки семантики (kitty-протокол, unsafe-инварианты).
- **Ошибка процесса, зафиксированная честно**: запустил `cargo fmt --all` по
  всему дереву, не дождавшись завершения параллельной сессии `crush`,
  работавшей над одним из тех же файлов. Гонка не привела к потере данных
  (сессия успела внести правку первой), но решение было поспешным — впредь
  дожидаться завершения активной сессии `crush` перед любыми
  дерево-модифицирующими операциями, даже механическими.

## Open questions

- Немедленный следующий шаг: прочитать `.crush/stdin/fix-config-unsafe.out`,
  проверить содержательную правку `version.rs`/`config_impl.rs` (linux-таргет,
  тесты, clippy) самостоятельно, решить — коммитить formatting и unsafe-фикс
  вместе или раздельно.
- #368: решение по способу фикса (gate тест / убрать appdata из Debug /
  два снимка) не принято.
- #370, #371: расследования проведены, самих правок (процедурная отрисовка
  символов; субпиксельное сглаживание в WGSL) не начато — крупный объём
  работы, не тривиальный фикс.
- #372: причина падения не установлена, ждёт согласования пользователя на
  возобновление расследования.
- Мусор в корне репозитория (`.claude/`, `ci/dev-install.ps1`) — решение
  пользователя.
- Установленная в систему сборка сильно отстала от HEAD; пуш не делался ни
  разу за всю сессию.

## Repo state

```
 M crates/bidi/src/bidi_context.rs
 M crates/bidi/src/lib.rs
 M crates/codec/src/lib.rs
 M crates/codec/src/messages/mod.rs
 M crates/codec/src/pdu.rs
 M crates/color-types/src/linear_rgba.rs
 M crates/config/src/color.rs
 M crates/config/src/color/color_scheme.rs
 M crates/config/src/color/tabbar.rs
 M crates/config/src/color/window_frame.rs
 M crates/config/src/config.rs
 M crates/config/src/config_impl.rs
 M crates/config/src/config_types.rs
 M crates/config/src/configuration.rs
 M crates/config/src/dynamic_convert.rs
 M crates/config/src/font_weight.rs
 M crates/config/src/keyassignment/launcher.rs
 M crates/config/src/keyassignment/spawn.rs
 M crates/config/src/lib.rs
 M crates/config/src/text_style.rs
 M crates/config/src/version.rs
 M crates/filedescriptor/src/lib.rs
 M crates/fontconfig/src/pattern.rs
 M crates/lfucache/src/test.rs
 M crates/mux/src/domain.rs
 M crates/mux/src/domain/writer_wrapper.rs
 M crates/mux/src/lib.rs
 M crates/mux/src/localpane/tests.rs
 M crates/mux/src/pane/test.rs
 M crates/mux/src/tab.rs
 M crates/mux/src/tab/test.rs
 M crates/mux/src/termwiztermtab/test.rs
 M crates/mux/src/tmux_commands/tests.rs
 M crates/pty/src/cmdbuilder/mod.rs
 M crates/pty/src/cmdbuilder/unix.rs
 M crates/term/src/terminalstate/mod.rs
 M crates/term/src/terminalstate/performer.rs
 M crates/termwiz/src/terminal/windows/mod.rs
 M crates/onlyterm-cell/src/attribute_change.rs
 M crates/onlyterm-cell/src/attributes.rs
 M crates/onlyterm-cell/src/cell.rs
 M crates/onlyterm-cell/src/lib.rs
 M crates/onlyterm-cell/src/test.rs
 M crates/onlyterm-cell/src/unicode.rs
 M crates/onlyterm-client/src/client/conn.rs
 M crates/onlyterm-client/src/client/mod.rs
 M crates/onlyterm-client/src/client/unilateral.rs
 M crates/onlyterm-client/src/domain/config.rs
 M crates/onlyterm-client/src/domain/inner.rs
 M crates/onlyterm-client/src/domain/mod.rs
 M crates/onlyterm-client/src/pane/renderable/hydrate.rs
 M crates/onlyterm-client/src/pane/renderable/mod.rs
 M crates/onlyterm-client/src/pane/renderable/state.rs
 M crates/onlyterm-escape-parser/src/apc/test.rs
 M crates/onlyterm-escape-parser/src/csi/mod.rs
 M crates/onlyterm-escape-parser/src/csi/mouse.rs
 M crates/onlyterm-escape-parser/src/csi/test.rs
 M crates/onlyterm-escape-parser/src/osc/test.rs
 M crates/onlyterm-escape-parser/src/parser/mod.rs
 M crates/onlyterm-escape-parser/src/parser/test.rs
 M crates/onlyterm-escape-parser/src/tmux_cc/mod.rs
 M crates/onlyterm-escape-parser/src/tmux_cc/tests.rs
 M crates/onlyterm-font/src/lib.rs
 M crates/onlyterm-font/src/parser/scan.rs
 M crates/onlyterm-font/src/shaper/rustybuzz/test.rs
 M crates/onlyterm-gui/src/customglyph/types.rs
 M crates/onlyterm-gui/src/glyphcache/glyph_cache_impl.rs
 M crates/onlyterm-gui/src/glyphcache/image_decode.rs
 M crates/onlyterm-gui/src/glyphcache/mod.rs
 M crates/onlyterm-gui/src/overlay/copy/key_tables.rs
 M crates/onlyterm-gui/src/overlay/copy/mod.rs
 M crates/onlyterm-gui/src/overlay/copy/pane.rs
 M crates/onlyterm-gui/src/overlay/copy/render.rs
 M crates/onlyterm-gui/src/termwindow/actions.rs
 M crates/onlyterm-gui/src/termwindow/mod.rs
 M crates/onlyterm-gui/src/termwindow/render_pipeline.rs
 M crates/onlyterm-gui/src/termwindow/webgpu/mod.rs
 M crates/onlyterm-mux-server-impl/src/sessionhandler/mod.rs
 M crates/onlyterm-mux-server-impl/src/sessionhandler/per_pane.rs
 M crates/onlyterm-mux-server-impl/src/sessionhandler/spawn.rs
 M crates/onlyterm-surface/src/cursor.rs
 M crates/onlyterm-surface/src/line/doubleclick.rs
 M crates/onlyterm-surface/src/line/zone.rs
 M crates/onlyterm-surface/src/position.rs
 M crates/onlyterm-surface/src/surface.rs
 M crates/window/src/egl/mod.rs
 M crates/window/src/egl/wrapper.rs
 M crates/window/src/os/windows/window.rs
?? .claude/
?? ci/dev-install.ps1
```

Примечание: все 90 `M`-файлов выше — некоммиченный дифф из `cargo fmt --all`
(формат) плюс правка сессии `fix-config-unsafe` в `version.rs`/
`config_impl.rs` (содержание). Ничего из этого не проверено и не
закоммичено на момент записи чекпоинта.

```
e358f2104 docs: checkpoint after completing all clippy and file-split tasks
72d607bc9 docs: changelog entries for the toast fix, clippy cleanup, and file split
b89099930 termwiz: split three largest files into modules (task #351)
a24ae48a0 pty, lfucache, onlyterm: split three files into modules (task #347)
260ce8704 onlyterm-client, onlyterm-mux-server-impl: split four largest files (task #346)
```
