# Checkpoint — 2026-08-06 12:41 [full-todo-list-closed]

## Session summary

This checkpoint closes out the entire visible TaskList for this long multi-day OnlyTerm (Windows-focused onlyterm fork) session, driven throughout by `/babygoal` + `/babysit` (cron `eac6d1f1`, 15-min interval, still armed). Three consecutive rounds were completed and independently verified this window, each ending in an `@oh` (or, for the last item, `@sh` via a `/crush` fallback) review pass:

**Round 1 — OpenGL/glium/EGL/WGL removal + a font/config bug-fixing batch** (tasks #413-417, #418, #420-422, #426-428): fully closed, documented in `docs/checkpoints/2026-08-06-0945.md`. Two full `@oh` review passes were needed — the first found a critical packaging break (dead ANGLE/Mesa DLL references, ~42MB) and a process-wide-kill bug in the new WebGpu-failure handling; the second pass (after fixing those) found a race condition and a window leak still remaining in the WebGpu-failure fix, which needed one more iteration to close for real.

**Round 2 — the older #399-401/#405/#407 startup-UX chain**: fully closed, documented in `docs/checkpoints/2026-08-06-1103.md`. The headline result: task #407 (white/transparent rectangles + startup flicker when a second OnlyTerm window overlaps another) was finally root-caused for real, after an earlier session's alpha_mode "fix" was independently proven to be a no-op on Windows (DX12 `SurfaceTarget::WndHandle` surfaces only ever report `Opaque`). The actual mechanism: the GDI startup placeholder was torn down as soon as a frame was *enqueued* to the render thread, not once it was actually *presented* -- closed by deferring the clear to `renderthread.rs`'s `submit_one_frame`. A follow-up `@oh` pass on this fix found the round's own new comment only updated one of seven call sites describing the mechanism (one of which stated the literal opposite of the new behavior) -- fixed all six stragglers in a follow-up commit.

**Round 3 — task #402**, the last item on the list: a real (if narrow, GDI/USER-exhaustion-only) double-free in `Window::new_window`'s `CreateWindowExW` failure path, inherited from upstream (last touched 2022). User explicitly authorized fixing it and asked for test coverage of the edge cases. First attempt was dispatched to `/crush` (provider `zai`), which refused due to a peak-hours window (08:00-12:00) -- task #369's pre-armed crush-fallback rule ("switches on peak-hours or hard-limit") kicked in as designed, and the same full brief was handed to `@sh` instead. Fixed via a `Cell<bool>` idempotency flag (`extra_ref_reclaimed_by_ncdestroy`) following an existing pattern already used elsewhere in the same file, with 3 new unit tests directly exercising both failure orderings through the real `rc_to_pointer`/`take_rc_from_pointer` primitives. Independently re-verified in full (read the entire diff, re-ran build/clippy/fmt/test myself).

**Current instruction, being executed right now**: update the changelog (done -- added a #402 entry to the existing `docs/changelog.md`), write this checkpoint (in progress), commit all `.md` files via `git commit --amend` onto the current HEAD (`5d635532f`, the #402 fix commit) rather than a new commit -- explicitly requested by the user, and safe here since this entire branch (14+ commits) has never been pushed to `origin/main` yet -- then `git push`. This is the first `push` of this session; every prior action was purely local per the standing "never push without explicit request" rule, which the user is now explicitly lifting for this one action.

Every commit across all three rounds was independently rebuilt/relinted/retested by me (`cargo build --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo fmt --all -- --check`, and the relevant crates' test suites) before being accepted -- this session's standing zero-trust methodology held throughout, including for `@oh`'s own review findings (I independently re-verified the DX12 `composite_alpha_modes` claim by reading `wgpu-hal` source myself before accepting it).

## Active goal

No `/goal` Stop hook. Session driven by the standing `/babygoal` Stop-hook condition from earlier in the session ("реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу") plus the armed `/babysit` cron `eac6d1f1`.

## TaskList

### pending
- #369 -- persistent "crush-fallback state" marker (never complete). Just exercised for real this session: peak-hours refusal from `zai` correctly triggered the pre-armed fallback to `sh`.
- #408 -- persistent "СТОП-ПРАВИЛО: никогда не убивать чужие процессы OnlyTerm" marker (never complete).

### recently completed (this session, in order)
- #413-417 -- OpenGL/glium/EGL/WGL removed entirely from the codebase.
- #418 -- final round for the above: changelog, checkpoint, `.md` commit, two `@oh` review passes.
- #420-422, #426 -- font/config fixes (portability docs, missing-glyph warning hint, stack-overflow investigation, DirectWrite stretch-matching fix).
- #427 -- critical packaging fix (dead ANGLE/Mesa DLL references removed, ~42MB of assets deleted).
- #428 -- WebGpu-init-failure blast-radius fix, iterated to close a race condition and a window leak.
- #399-401 -- fade-interrupted-by-any-move fix, plus two comment-accuracy findings (one needed no code change).
- #407 -- the real root cause of the startup-flicker/transparent-rectangles bug, finally found and fixed.
- #409-411 -- final round for the #399-401/#405/#407 chain: changelog, checkpoint, `.md` commit, `@oh` review (which itself required one follow-up doc-comment-accuracy commit).
- #402 -- the double-free fix, via a `/crush`→`@sh` fallback, with 3 new regression tests.

TaskList is now down to just the two permanent standing markers (#369, #408) -- no actionable work remains.

## Decisions

- User explicitly authorized amending the current HEAD commit (rather than the session's usual "new commit, never amend" default) to fold in this round's `.md` changes, and explicitly authorized a `git push` -- the first of this session. Both are one-time, explicit overrides of standing defaults, not a change to those defaults going forward.
- Task #369's crush-fallback rule was exercised for real (peak-hours refusal) rather than just sitting dormant -- confirms the mechanism works as designed: detect the specific refusal class, don't retry/route around it, silently switch to the pre-armed fallback agent with the same task brief.
- Every `@oh`/`@sh` review finding in this window was independently re-verified against primary sources before being accepted or acted on (e.g. reading `wgpu-hal`'s actual source for the DX12 alpha-mode claim, tracing the actual `rc_to_pointer`/`take_rc_from_pointer` call graph for the double-free claim) -- not taken at face value from any agent's self-report, including this session's own most-trusted reviewer role.

## Open questions

Carried over, unresolved, from earlier checkpoints (not touched this window):
- Whether "OpenGL removal shifted the startup call-stack depth enough that task #422's original crash no longer reproduces" is actually correct -- never verified directly.
- Task #426's literal reported trigger (system DPI-scale change followed by terminal zoom) was never reproduced in isolation -- only the terminal-zoom half was tested, across two separate investigation attempts.

No new open questions from this window's work -- #407's fix is now on a solid structural footing (verified by two independent code readings across two different reviewers), and #402's fix has real, non-vacuous test coverage of both failure orderings.

## Repo state

```
 M docs/changelog.md
?? .claude/
?? ci/dev-install.log
```

```
5d635532f window: fix double-free of the extra Rc ref on a create_window failure after WM_NCCREATE (task #402)
2acd837c1 docs: fix stale placeholder-clear comments found by @oh's review (task #411)
db47e078b docs: checkpoint closing out the #399-401/#405/#407 round (task #410)
7c5c3d014 docs: changelog for the #399-401/#405/#407 startup-UX round (task #409)
1ab9d2cd7 window: fix inaccurate DeleteObject SAFETY comment for PlaceholderSpinner (task #400)
```

Branch is 20 commits ahead of `origin/main`, none pushed yet as of this checkpoint.
