# Checkpoint — 2026-07-24 21:30 [onlyterm-decouple-from-C + upstream-bug-harvest]

## Session summary

Эта сессия — прямое продолжение предыдущей (см. `docs/checkpoints/2026-07-24-1800.md` для полной
истории инициативы «убрать весь C/C++ из onlyterm», которая была полностью завершена: cairo→tiny-skia,
freetype/harfbuzz→rustybuzz/swash, Lua(mlua)→rhai, SSH/TLS-mux/git2 удалены, rusqlite→redb).
В начале этой сессии по просьбе пользователя весь стек был запушен в `origin` (форк
`PHPCraftdream/onlyterm`), и исправлены два реальных бага, которые пользователь сам репортил в
апстрим: **#7963** (паника в fallback-шрифте при ACL-Denied файле, коммит `5752050b8`) и **#7965**
(STATUS_STACK_OVERFLOW в onlyterm-gui.exe — точная причина не найдена, но превентивно отвердили
рекурсивные операции над деревом процессов в `procinfo`, коммит `0894be2c4`, синтетический тест на
20000 уровней). Оба коммита на `main`, оба запушены в `origin`. CI на форке **не активирован**
(`gh api repos/PHPCraftdream/onlyterm/actions/runs` → `total_count: 0`) — GitHub Actions на форках
требует ручной активации через веб-UI, программно не обойти.

Основная часть ЭТОЙ сессии — большое исследование апстрима (`wezterm/wezterm`) на предмет полезных
багфиксов для переноса в форк. Прошло в три волны, каждая через параллельных `@sx` (Sonnet 5, xhigh
effort) под-агентов:

1. **Первая волна** (3 агента) — широкий обзор: открытые PR (280 шт.), открытые bug-issues (952 шт.,
   не получилось объять все — агенты честно сузили через фильтрацию по меткам/ключевым словам),
   коммиты апстрима после точки расхождения (оказалось — апстрим не продвинулся НИ НА ОДИН коммит с
   момента форка, 16 июля 2026, `merge-base == upstream/main HEAD` — переносить из свежих коммитов
   нечего). Я лично проверил вручную 2 топовых находки (codec PDU allocation, LruCache::unbounded) —
   обе подтвердились дословно в реальном коде.

2. **Пользователь справедливо не поверил, что агенты "исследовали" сотни PR/issues за 8 минут** —
   я честно объяснил методологию (bulk-список → дешёвая фильтрация по меткам → глубокое чтение только
   шорт-листа), и по явной просьбе пользователя вытянул и показал ПОЛНЫЕ сырые списки заголовков:
   все 280 PR (`docs/upstream-research/open-prs-titles.txt`) и все 952 bug-issues, отсортированные по
   комментариям (`docs/upstream-research/open-bug-issues-by-comments.txt`).

3. **Вторая волна** (3 параллельных `@sx` агента, TRIAGE-PR/ISSUES-1/ISSUES-2) — построчный, полный
   (без семплирования) перебор ВСЕХ 280 PR и ВСЕХ 952 issues, классификация каждой записи на
   "важное" (баг/крэш/тормоз/плохой код) vs "неважное" (фича/косметика/вопрос). Результат — PR-триаж
   получился избирательным (74/280, с проверкой прямо в исходниках через `git grep`/`gh pr view`),
   issues-триаж — честно широким (~800/952 помечены "важное", т.к. критерий был "баг vs не баг", а не
   "критично vs мелочь"). Файлы: `triage-prs.md`, `triage-issues-part1.md`, `triage-issues-part2.md`.

4. **Третья волна** (3 параллельных `@sx` агента, CLUSTER-1/2/3) — пользователь попросил
   сгруппировать похожие записи по общему первопричинному механизму (много дублей среди ~886
   "важных" записей). Каждый агент прочитал ВСЕ ТРИ файла триажа целиком и сгруппировал свою
   категорию (crash/hang/leak — 19 кластеров; rendering/font/perf — 28 кластеров;
   input/window/focus/clipboard — 29 кластеров, итого 76). Файлы: `clusters-crashes.md`,
   `clusters-rendering-perf.md`, `clusters-input-window.md`. Побочные находки кластеризации: два PR
   (#5969, #7199), изначально ошибочно помеченные "фича" в PR-триаже, оказались готовыми фиксами
   реальных функциональных багов — переклассифицированы.

5. **Финальный синтез** (сделан МНОЙ лично, без под-агентов, по прямой команде пользователя через
   `/oh`) — прочитал все три кластерных файла, вручную дедуплицировал пересекающиеся кластеры (одни и
   те же PR/issues группировались по-разному в разных файлах — например "GPU teardown crash" и
   "PaneFocused storm" встречались идентично в двух файлах), и построил единый ранжированный список
   `docs/upstream-research/MASTER-PRIORITY-LIST.md`: **Tier 0** (30 пунктов — готовый PR-фикс в
   апстриме, дешёвый перенос) + **Tier 1** (20 пунктов — механизм понятен/подтверждён, но фикса в
   апстриме нет, нужна собственная работа) + Tier 2/3 (бэклог, оставлены как списки в кластерных
   файлах без отдельных тасков). На каждый из 50 пунктов Tier 0+1 заведена задача в TaskList (`#74`–
   `#123`, `UP-01`…`UP-50`) — **ни одна ещё не начата**, все `pending`, работа над ними не
   стартовала в этой сессии.

**Методологическая заметка**: в середине предыдущей сессии пользователь переключал методологию
реализации между `@sh` (Agent-тул) и `crush run` (внешний CLI) туда-обратно несколько раз; последнее
явное указание в ЭТОЙ сессии — "переключаемся обратно на агентов `@sh`" — то есть **текущий режим
реализации: `@sh` через Agent-тул**, не `crush`. Это не переподтверждалось после исследовательской
части этой сессии (вся работа с #74–#123 была чисто research/triage, без единого изменения кода),
так что при старте реализации UP-01+ следует держаться `@sh`, если пользователь явно не скажет иначе.

**Активных worktree, кроме основного, нет** (`git worktree list` — только `main`). `main` синхронизирован
с `origin/main` по состоянию на коммит `0894be2c4` — но **два новых коммита от 2026-07-24
(font-fallback фикс #7963, procinfo hardening #7965) нужно перепроверить, пушились ли они повторно
после первого `git push origin main`** (в разговоре пуш был один явный, до этих двух коммитов и после
— не переподтверждён отдельным вызовом `git push`, хотя ассистент предлагал; свериться с
`git log origin/main` в начале следующей сессии, прежде чем предполагать, что удалённый форк
актуален).

## Active goal

`/goal`-условие из самого начала цепочки сессий («реализуй через @sh, коммиты между задачами, тесты,
тулинг в первую очередь») — пользователь в предыдущей сессии подтвердил решение "снять /goal", но
сама команда `/goal clear` — user-side, ассистент не может вызвать её программно; **не подтверждено,
выполнил ли пользователь её фактически**. Если Stop-hook снова начнёт блокировать завершение хода со
ссылкой на «@sh», это тот же нерешённый хвост из прошлой сессии — сейчас неактуально, поскольку весь
объём этой сессии был research/triage (без кодовых изменений), под которые условие `/goal` в любом
случае не применялось напрямую.

## TaskList

### pending (50 задач, ни одна не начата)
- #74 UP-01. [T0] Mux PDU OOM: bound data_len allocation (PR #7874↔#7527) — highest priority, дословно подтверждено в коде
- #75 UP-02. [T0] SGR 8-bit цвет: двоеточие→точка-с-запятой (PR #2724↔#2723)
- #76 UP-03. [T0] Sixel: пропущенные параметры цвета → 100 вместо 0 (PR #7345↔#7344)
- #77 UP-04. [T0] WebP lossy-декодирование не работает (PR #7694↔#7693)
- #78 UP-05. [T0] LruCache::unbounded() → неограниченный рост памяти (PR #7704)
- #79 UP-06. [T0] Windows: deadlock при смене раскладки клавиатуры (PR #7066↔#6786)
- #80 UP-07. [T0] BlobLease Access Denied + утечка/hang (PR #7955 + #5422/#7285/#6426)
- #81 UP-08. [T0] Bracketed-paste defang security-баг (PR #7859)
- #82 UP-09. [T0] Mux detach/reattach теряет user_vars (PR #7610↔#5832)
- #83 UP-10. [T0] Mux detach/reattach теряет mouse-reporting (PR #6782↔#6764)
- #84 UP-11. [T0] Background-изображение шов на стыке (PR #7030↔#6335)
- #85 UP-12. [T0] top_level-сплит ghost space (PR #5969↔#4984/#4686)
- #86 UP-13. [T0] INTEGRATED_BUTTONS overlap (PR #7199↔#7197)
- #87 UP-14. [T0] Portable-pty exec() abort() (PR #7743)
- #88 UP-15. [T0] Windows gui-sock discovery path (PR #7896+#7698)
- #89 UP-16. [T0] --attach не долетает до domain_spawn_v2 (PR #7583↔#7582)
- #90 UP-17. [T0] Windows PATHEXT ";;" паника (Issue #6499)
- #91 UP-18. [T0] GPU-контекст teardown crash macOS/X11 (PR #7958+#7799+#7617)
- #92 UP-19. [T0] WebGPU текстура-лимит (PR #7821↔#7819)
- #93 UP-20. [T0] Wayland DPI-calc фиксы (PR #7954+#7966+#7548)
- #94 UP-21. [T0] Windows drag-lock/resize-loop DPI (PR #7883)
- #95 UP-22. [T0] Windows conpty teardown hang + procinfo argv0 (PR #5977+#7226+#7398)
- #96 UP-23. [T0] Mux client-server deadlock много панелей (PR #7771+#7929)
- #97 UP-24. [T0] Kitty keyboard Composed-клавиши (PR #7944/#7915+#7542)
- #98 UP-25. [T0] Wayland дублирование клавиш (PR #7435)
- #99 UP-26. [T0] Key-repeat Wayland таймер (PR #7487)
- #100 UP-27. [T0] PaneFocused notification storm — топ-приоритет (PR #7871)
- #101 UP-28. [T0] Выделение мышью CJK wide-char (PR #7930↔#6733)
- #102 UP-29. [T0] Pane move/swap/rotate non-local domains (PR #5009)
- #103 UP-30. [T0] Mux бесконечный цикл ресайза панелей (PR #6062)
- #104 UP-31. [T1] Крэш при экстремальном scale factor
- #105 UP-32. [T1] Multi-monitor DPI drag — macOS/X11
- #106 UP-33. [T1] macOS сон/пробуждение ломает окно на внешнем мониторе
- #107 UP-34. [T1] Порча рендера при ресайзе во время вывода (давний, топ по частоте)
- #108 UP-35. [T1] Курсор после ресайза+reflow в alt-screen
- #109 UP-36. [T1] Mux ресайз панелей рассинхронизирован (10 issues)
- #110 UP-37. [T1] window_decorations RESIZE|TITLE bitflags баг
- #111 UP-38. [T1] IME-композиция хрупкий конвейер
- #112 UP-39. [T1] Idle/высокая CPU — VTParser::action hotspot
- #113 UP-40. [T1] Тормоза скролла TUI
- #114 UP-41. [T1] Медленный старт на Windows (root cause в #7782)
- #115 UP-42. [T1] Фриз GUI при большом выводе (AI coding-инструменты — релевантно нам самим)
- #116 UP-43. [T1] Фокус/мышь рассинхронизация событий
- #117 UP-44. [T1] Спонтанное дублирование окон/вкладок
- #118 UP-45. [T1] Clipboard асинхронные гонки
- #119 UP-46. [T1] Живая перезагрузка темы не ко всем окнам
- #120 UP-47. [T1] Portable-pty оставшиеся крэши
- #121 UP-48. [T1] Wayland протокольные крэши
- #122 UP-49. [T1] Необработанные OS-ошибки валят процесс
- #123 UP-50. [T1] Verification-тест #6157 (вероятно уже исправлен)

### recently completed (из этой + предыдущей сессии, контекст)
- Полная инициатива «убрать C/C++» (cairo/freetype/harfbuzz/mlua/SSH/TLS/git2/rusqlite) — все треки
- FIX-7963, FIX-7965 (BUG8-класс) — оба смержены и в теории запушены (см. открытые вопросы)

## Decisions

- Единый список Tier 0/Tier 1 составлен ВРУЧНУЮ ассистентом (не агентом) при синтезе трёх кластерных
  отчётов — дедупликация пересекающихся кластеров сделана лично, не автоматизирована.
- Tier 2/Tier 3 (средний/низкий приоритет) сознательно НЕ получили отдельных задач в TaskList —
  оставлены как справочные списки в файлах кластеризации, чтобы не раздувать TaskList на 76+ пунктов;
  могут быть заведены по отдельному запросу пользователя.
- Для кластеров с несколькими конкурирующими апстрим-PR на один и тот же баг (PaneFocused: #7871 vs
  #7763/#6349/#7590; Kitty Composed: #7944 vs #7915) — выбирать только ОДНУ, самую зрелую итерацию,
  не переносить/пытаться совместить несколько параллельно.
- Режим реализации на конец сессии — `@sh` (Agent-тул), не `crush run` (последнее явное указание
  пользователя в этой сессии).

## Open questions

- Не подтверждено: были ли смержены/запушены в `origin` коммиты #7963 (`5752050b8`) и #7965
  (`0894be2c4`) ПОСЛЕ первого `git push origin main` в этой сессии, либо они уже входили в тот самый
  пуш. Свериться с `git log origin/main -3` в начале следующей сессии, прежде чем предполагать
  синхронность.
- GitHub Actions на форке `PHPCraftdream/onlyterm` всё ещё не активирован — пользователю нужно включить
  вручную через веб-UI, ассистент не может сделать это программно.
- `/goal clear` — не подтверждено, что пользователь фактически выполнил эту user-side команду.
- Порядок реализации 50 заведённых задач (UP-01...UP-50) не согласован явно — последний ответ
  ассистента предложил начать с Tier 0 через `@sh`, но пользователь не подтвердил ни порядок, ни то,
  что начинать нужно прямо сейчас (сессия прервана командой `/checkpoint`).
- Кластер "GPU-контекст teardown" (UP-18) и DPI/scale-кластеры (UP-19/20/21, UP-31/32) частично
  пересекаются механизмом — при реализации стоит проверить, не закрывает ли один фикс сразу оба
  (отмечено как открытый вопрос и в самом `MASTER-PRIORITY-LIST.md`).

## Repo state

```
?? docs/checkpoints/
?? docs/plans/
?? docs/upstream-research/
```

```
0894be2c4 procinfo, mux: make LocalProcessInfo tree ops iterative, not recursive
5752050b8 onlyterm-font: skip unreadable fallback font candidates instead of erroring out
e3cd2b291 docs: Lua-to-rhai migration guide (L6)
48e38e224 config, lua-api-crates, mux, onlyterm-gui: remove mlua and luahelper from workspace (L5)
0032893ae Merge phase L4.7: port debug REPL overlay from mlua to rhai
33079d01a onlyterm-gui: port debug REPL overlay from mlua to rhai (L4.7)
```

Новые файлы этой сессии (все untracked, не закоммичены — рабочие артефакты исследования):
`docs/upstream-research/{open-prs-titles.txt, open-bug-issues-by-comments.txt,
issues_chunk_00, issues_chunk_01, triage-prs.md, triage-issues-part1.md, triage-issues-part2.md,
clusters-crashes.md, clusters-rendering-perf.md, clusters-input-window.md,
MASTER-PRIORITY-LIST.md}`.
