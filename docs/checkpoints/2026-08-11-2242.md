# Checkpoint — 2026-08-11 22:42 [windows-058-migration-plus-two-live-regressions]

## Session summary

Direct continuation of the `2026-08-11-1912.md` checkpoint (same session). That checkpoint
closed out the #511-520 round (device-lost hardening + the first startup-flicker fix,
`b57aa340b`) with a clean `@oh` review. This window covers: a slow-startup investigation, a
`crush`-assisted Alt+V fix, a `windows` crate migration now underway, and — critically — two of
this window's own "fixed" bugs being reported by the user as still broken on a byte-identical
rebuild, which reopened both as active investigations.

**Slow-startup investigation** (release build, not debug): added permanent `startup:` log
checkpoints (`ae72aec80`) across `main.rs`/`context.rs`/`window.rs`, profiled via
`ONLYTERM_LOG=info,wgpu_core=debug,wgpu_hal=debug`. Found the real cost split: ~2s in DXGI
adapter enumeration (hybrid-graphics laptop — probing the suspended NVIDIA RTX 3050 Ti to check
its capabilities wakes it, even though Intel UHD Graphics is what's actually selected under the
default `LowPower` preference) and ~1.2s in FXC shader compilation. Tried switching to DXC
(`Dx12Compiler::DynamicDxc`, vendored `dxcompiler.dll`/`dxil.dll` the same way `conpty.dll` is
already vendored) — measured **no real win** across two runs (if anything marginally slower for
our own shader), so that whole experiment was cleanly reverted (task #522, closed with a negative
result, no code landed). The DXGI-enumeration cost was research-only this round (task #523,
`8a93e7e32`): concluded there's no safe fix via wgpu's public API — the one function that could
build a single-adapter `ExposedAdapter` (`dx12::Adapter::expose`) is `pub(super)` inside
`wgpu-hal`, not exported, and even if it were, `wgpu-hal` 25.0.2 depends on `windows = "0.58"`
while this workspace was pinned to `0.33` (incompatible types). Recommended: accept the cost,
file upstream, or fork `wgpu-hal` — explicitly NOT recommended without a separate user decision.

**Alt+V fix, round 2** (task #524, `f88d36713`): tried `crush` for the first time this session on
two parallel tasks (this one and the DXGI research). Both `crush` invocations initially failed on
a `--cwd` backslash-mangling bug in this Bash environment (fixed by using forward slashes), then
DXGI-research crush ended with an empty message and no deliverable (I wrote that plan myself
instead), while the Alt+V crush attempt produced a real, logically-correct diff (verified by
reading it against the two correctness traps — AltGr exclusion, SHIFT-normalized twin — both
handled right) but crush itself never ran `cargo build` and never wrote the required test before
stalling. I built/clippy/fmt-verified it myself and wrote the three required tests
(`alt_v_user_binding_resolves_via_physical_key_regardless_of_layout` and two negative cases).

**Then the user asked about GPU selection**, which surfaced that `webgpu_power_preference` (a
pre-existing config option) already lets users force `HighPerformance` — no new code needed for
that part. User then explicitly chose to (a) vendor+patch `wgpu-hal` to actually implement the
DXGI-enumeration bypass despite my explicit non-recommendation, and (b) do a **full workspace
migration from `windows = "0.33.0"` to `"0.58"`** as a prerequisite (not just a scoped
second-dependency workaround, which is what I'd suggested as the lower-risk option). Measured the
actual `windows::` surface first (only 4 files, ~13 call sites — far smaller than feared) and
started the migration directly: root `Cargo.toml` bumped, `onlyterm-toast-notification`'s WinRT
toast code fixed (`&`-borrow changes windows-rs 0.58 now requires, `WinError::new` takes `&str`
directly), `window`'s `connection.rs` DXGI display-config code fixed (`QUERY_DISPLAY_CONFIG_FLAGS`
newtype wrapping, `WIN32_ERROR` newtype comparisons via renamed imports to avoid clashing with the
still-needed `winapi` constants for an unrelated call in the same file, `None` instead of
`std::ptr::null_mut()` for an `Option<*mut _>` param). `main.rs` and `window.rs`'s own WinRT usage
(`UI::Color`/`UIColorType`/`UISettings`) compiled unchanged. Whole-workspace build clean; clippy
clean on the three touched crates (`onlyterm-toast-notification`, `window`, `onlyterm-gui` —
`mux --all-targets` has a pre-existing, unrelated, already-documented clippy issue, not touched
this round); fmt now clean after one manual fix (a rustfmt-preferred single-line import collapse).
**Not yet committed** — this is the very next action.

**Then, mid-verification, the user reported two live regressions** on a freshly rebuilt+installed
binary: the startup flicker "not cured" (now also seen against video-playing windows, not just
other OnlyTerm windows — a new detail `b57aa340b` never accounted for) and Alt+V "still didn't
work" despite `f88d36713`. Investigated whether this was a stale-binary artifact before assuming
either fix is actually broken: confirmed via SHA256 hash that the installed
`C:\Program Files\OnlyTerm\onlyterm-gui.exe` is **byte-identical** to my own freshly-built
`target/release/onlyterm-gui.exe` (which contains every commit through `8a93e7e32` plus the
in-flight, uncommitted windows-0.58 migration diff) — so this is not a deployment/staleness issue,
both regressions are real and unexplained. Formed one new, unverified hypothesis for Alt+V before
being interrupted: **Alt+<letter> generates `WM_SYSKEYDOWN`/`WM_SYSCHAR` on Windows, not
`WM_KEYDOWN`/`WM_CHAR`** — if `do_wnd_proc` in `window.rs` doesn't route `WM_SYSKEYDOWN` through
the same `raw_key_event_impl` path as regular key messages, the whole `synthesize_physical_
fallbacks` fix from `f88d36713` would never even be reached for Alt+<letter>, since the
inputmap-level lookup is fine but the *event* never gets there. **Not yet checked against the
actual code** — this is the first thing task #525 needs to verify.

Then the user invoked `/babygoal` ("важно — решить все эти оставшиеся задачи") mid-flow while I
was investigating, decomposed 3 tasks (#525 Alt+V-round-2, #526 flicker-round-2, #527 finish/land
the windows migration), armed `/babysit` (cron `71c12649`, confirmed via `CronList`), chained
527→525→526, and started on #527 (nearly done, just needed the final verification pass — now
complete except for the commit itself). Then the user separately invoked `/task` (not
`/babygoal` — plan-only, do not execute) to register the wgpu-hal-vendoring plan as three more
tasks: #528 (vendor + patch visibility), #529 (implement the actual `EnumAdapterByGpuPreference` +
`create_adapter_from_hal` fast path with a mandatory safe fallback), #530 (verify the real
speedup with multiple runs, learning from the DXC episode's lesson that a single measurement
isn't trustworthy) — chained 527→528→529→530, per the `/task` skill's explicit "do not start
executing" contract, so these three are pending and untouched.

## Active goal

The Stop-hook from `/babygoal` remains armed (verbatim: "важно - решить все эти оставшиеся
задачи. Заведи таски и решай"). Satisfied once `TaskList` reaches 0 pending/in_progress. Currently
6 tasks open (#525, #526, #527 in_progress, #528, #529, #530), so nowhere close to done — do not
stop working once this checkpoint write completes.

## TaskList

### in_progress
- #527 Завершить и закоммитить миграцию windows 0.33 → 0.58 (build/clippy/fmt all clean now;
  commit is the only remaining step)

### pending
- #525 Alt+V всё ещё не работает после фикса inputmap.rs (blockedBy: #527) — WM_SYSKEYDOWN
  hypothesis formed, not yet checked
- #526 Мерцание при старте поверх других окон не исчезло после b57aa340b (blockedBy: #525) —
  diagnosis needs to be redone from scratch, not reapplied; new detail (also flickers against
  video windows, not just other OnlyTerm windows) not yet accounted for
- #528 Вендорить wgpu-hal 25.0.2 и патчить видимость expose()/D3D12Lib (blockedBy: #527)
- #529 Реализовать быстрый выбор GPU-адаптера через EnumAdapterByGpuPreference (blockedBy: #528)
- #530 Верифицировать обход перечисления: замеры + по возможности WPR (blockedBy: #529)

### recently completed
- #524 Alt+V physical-key-fallback fix, round 1 (`f88d36713`) — now suspected incomplete, see #525
- #523 DXGI adapter-enumeration research plan (`8a93e7e32`)
- #522 DXC shader-compiler experiment — negative result, cleanly reverted, no commit
- #521 startup: log checkpoints made permanent (`ae72aec80`)
- #520/#511-519: prior round, see `2026-08-11-1912.md`

## Decisions

- Went ahead with the user's explicit choice to fork/vendor `wgpu-hal` despite having recommended
  against it in the research doc — the recommendation was conditional on "without a separate
  explicit decision", and the user made that decision directly.
- Did the `windows` 0.33→0.58 migration as a genuine **full** migration (per the user's explicit
  ask) rather than the smaller "second dependency scoped to new code only" option I'd floated —
  measured the actual surface first (4 files, ~13 sites) before committing to it, which showed the
  full migration was far cheaper than my initial (pre-measurement) assumption.
- On discovering the two live-regression reports, treated them as genuine bugs requiring fresh
  investigation rather than re-asserting the prior diagnosis — explicitly checked binary staleness
  first (SHA256) before either assuming false-alarm or assuming the fix was wrong, since both are
  live possibilities and jumping to either without checking would waste the user's time.
- Chained the new wgpu-hal-vendoring tasks (#528-530) behind the windows migration (#527), since
  the vendored/patched code will need to interoperate with wgpu-hal's own `windows 0.58` types.
- Used `/task`'s plan-only contract literally — registered #528-530 without touching any code for
  them, even though `/babygoal`'s Stop-hook is armed and would normally push toward continuous
  work; the `/task` skill's explicit instruction ("do NOT start executing") takes precedence for
  those three specific tasks.

## Open questions

- **Is the `WM_SYSKEYDOWN` hypothesis for Alt+V actually correct?** Not yet checked against
  `window.rs`'s `do_wnd_proc`. First action item for #525.
- **What's actually causing the flicker if `b57aa340b`'s diagnosis was incomplete?** The new
  "also happens against video windows" detail suggests something broader than the WebGpu child
  window's specific `CS_HREDRAW`/`WS_VISIBLE` issue — needs a from-scratch look, not a patch on
  the existing theory.
- **Will the wgpu-hal vendoring (#528-530) actually pay off?** Given the DXC experiment's lesson
  (theoretically-sound optimization, zero measured benefit), #530 is designed to require multiple
  measured runs before declaring success, and to report honestly if it doesn't pan out — same as
  DXC was handled.
- Whether/when to push. `main` is now well ahead of `origin/main` (last push was after the
  round-2 checkpoint, many rounds ago) — not requested since then.

## Repo state

```
 M Cargo.lock
 M Cargo.toml
 M crates/onlyterm-toast-notification/src/windows.rs
 M crates/window/src/os/windows/connection.rs
?? .claude/
?? ci/dev-install.log
?? docs/checkpoints/2026-08-10-1942.md
?? docs/checkpoints/2026-08-10-2355.md
?? docs/checkpoints/2026-08-11-1554.md
```
(The windows-0.58 migration diff above is verified clean — build/clippy/fmt all pass — but not
yet committed; that's the immediate next action once this checkpoint write completes.
`docs/checkpoints/2026-08-11-2242.md`, this file, not shown above since captured just before
writing it.)

```
8a93e7e32 docs: research plan for DXGI adapter-enumeration wake-up cost
f88d36713 gui: user-configured Alt/Ctrl/Super keybindings now resolve on any keyboard layout
ae72aec80 gui,window: keep startup: log checkpoints as permanent diagnostics
3ee423d58 docs: changelog and checkpoint for the #511-515 round and the startup flicker fix
8f04d9973 gui: name which list a failed --start-conf command came from
cda74c67c gui: route send_frame's repaint_pending store through mark_repaint_pending
b57aa340b window: stop the WebGpu child window from flashing see-through on startup
277afe66c gui: keep webgpu's drop order in TermWindow::drop, don't take() it early
```

`origin/main` is at `45914bb4a` (pushed several rounds ago); `main` is many commits ahead, nothing
pushed since.
