# Checkpoint — 2026-07-29 21:00 [unsafe-audit-closed, execution-decoupling-started]

## Session summary
Прямое продолжение чекпоинта `2026-07-29-1700.md`. Пользовательский `/goal`: "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу" — этот цикл теперь полностью пройден для инициативы project-wide unsafe-аудита.

После предыдущего чекпоинта агент `@oh` (та же сессия, продолженная через `SendMessage`/`resumedAgentId`, не новый спавн) дважды перепроверял мои фиксы по итогам своего первого ревью:
- **Первая перепроверка** подтвердила все 4 коммита (`97b2f6383` uninit_vec, `b77a81899` NaN-фикс, `2dd032a24` 13 недокументированных unsafe + попутные deny-by-default clippy-баги, `f4e2d9268` 4 ложных SAFETY-комментария) корректны и эффективны, но нашла НОВУЮ проблему: `cargo clippy --workspace` всё ещё не проходит целиком — упирается в 8 предсуществующих `clippy::unused_io_amount` в `crates/term/src/terminalstate/{mod.rs,mouse.rs}` (тот же класс бага, что чинили в `termwiz/probed.rs`: молчаливая частичная запись могла обрезать ответ терминала приложению или, что более серьёзно нашлось при второй перепроверке, **обрезать paste пользователя** в `ThreadedWriter`, который специально блокируется для back-pressure).
- Это исправлено коммитом `d5d60d329` (все 8 `write()` → `write_all()`).
- **Вторая перепроверка** подтвердила: `cargo clippy --workspace` теперь ДЕЙСТВИТЕЛЬНО зелёный (проверено двумя независимыми прогонами с обходом кеша, 60 юнитов релинчено, 0 ошибок), `cargo test --workspace` — 694 passed/0 failed/3 ignored, 131 test suite. Инициатива unsafe-аудита формально закрыта агентом-ревьюером. Единственная оставшаяся мелочь (не блокер): 3 `transmute` без явной аннотации типа в `window.rs:191/217/566` — консистентность с уже исправленным паттерном в `filedescriptor`, не соundness-баг.

Чейнджлог дополнен короткой заметкой про ревью-фиксы, закоммичен вместе с этим чекпоинтом (`6d49b04f9`).

**Сразу после этого** пользователь (используя `/oh` для смены эффорта модели, не для вызова агента) указал на мою ошибку: при более раннем `/task`-вызове я ЗАМЕНИЛА план №192-200 (render-thread-per-window) вместо того, чтобы ДОПОЛНИТЬ его новой инициативой — пользователь настаивал, что просил именно "добавить задачи". Явно сформулирована новая (восстановленная по сути) цель: **разделить связность выполнения**, чтобы зависание одной вкладки не роняло ни окно, ни весь процесс, ни — что происходит СЕЙЧАС — вообще все окна/приложения пользователя одновременно. Плюс: повысить lock-free-характер работы программы, оценить перевод pty I/O на асинхронность.

Я прочитала актуальный код (не полагаясь на память из предыдущих частей сессии) и подтвердила root cause: (1) все окна живут в одном процессе через `gui-sock-{pid}`/`ONLYTERM_UNIX_SOCKET`; (2) один `run_message_loop()` (`window/os/windows/connection.rs:73`) на весь процесс; (3) `WM_PAINT` → `wm_paint()` → `WindowEventSender::dispatch` вызывается СИНХРОННО (`window/src/lib.rs:243-248`) → `TermWindow::do_paint()` — реальные GPU-вызовы прямо в message loop; (4) `LocalPane.terminal: Mutex<Terminal>` (`mux/src/localpane.rs:153`) контендится рендером/pty-парсером/вводом одновременно, с уже существующей инструментацией `lock_terminal_timed`/`localpane.terminal_lock.wait.*`; (5) никакого watchdog/heartbeat/детектора зависания в проекте нет.

Написан план `docs/plans/2026-07-29-execution-decoupling.md` (уровни A-F: watchdog → lock-free данные → рендер-поток WebGPU → рендер-поток OpenGL → супервизия/убийство вкладки с дампом → опция «окно=отдельный процесс» → async-оценка pty I/O) и заведены задачи **#218-226** с корректными `blockedBy`-зависимостями. План и задачи оставлены НЕ РЕАЛИЗОВАННЫМИ — пользователь ещё не выбрал, с какой задачи начинать, когда сессия была прервана очередным срабатыванием stop-hook.

## Active goal
Session-scoped Stop hook (от исходного `/babygoal`): "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу". **Условие теперь полностью выполнено для инициативы unsafe-аудита** (все 4 шага пройдены, ревью `@oh` дважды подтвердило зелёный результат). Новая инициатива (#218-226) НЕ подпадает под этот goal — она не была częscią исходного запроса и пользователь ещё не давал команду её начинать.

## TaskList
### pending (новая инициатива, НЕ начата)
- #218 Watchdog GUI-потока: heartbeat + детект залипания
- #219 Снимок строк для рендера: убрать GPU-работу из-под Mutex<Terminal>
- #220 Lock-free публикация ui_items: ввод и хит-тестинг не ждут рендер
- #221 Рендер-поток на окно (WebGPU): вынести GPU из message loop (blockedBy: #219, #220)
- #222 Рендер-поток (OpenGL): создание GL-контекста на рендер-потоке (blockedBy: #221)
- #223 Супервизия вкладок: изолированное убийство зависшей вкладки с дампом (blockedBy: #218, #221)
- #224 Опция «окно = отдельный процесс» вместо attach к gui-sock
- #225 Async-оценка: pty reader+parser (2 потока на пейн) → async (blockedBy: #219)
- #226 Регресс-тест изоляции: искусственно зависший GPU и зависший пейн (blockedBy: #221, #223)
- #213 crush-fallback state (persistent — do not complete) — STATUS: armed, AGENT: sh (вооружён, но не активен; переключение на sh произойдёт только при реальном peak-hours/hard-limit отказе crush)

### recently completed
- #217, #216, #215, #214 — фиксы по итогам ревью @oh (все закоммичены и дважды перепроверены)
- #202-212 — весь unsafe-аудит + первичное ревью

## Decisions
- Unsafe-аудит закрыт официально: `cargo build/clippy/test --workspace` зелёные на некешированных прогонах, подтверждено независимым агентом `@oh` дважды.
- Новая инициатива (execution decoupling) — ДОПОЛНЯЕТ, а не заменяет прошлые решения; пользователь явно поправил ошибочную замену плана.
- Порядок работ: watchdog → lock-free данные → рендер-поток (WebGPU затем OpenGL) → супервизия/kill-with-dump → опция per-process → async-оценка (последней, начинается с замеров, не с переписывания).
- Ранее принятое решение подтверждено снова: НЕ делать сквозной Rc→Arc рефакторинг для рендер-потока — рендер-поток строит свой Rc-мир из Send-safe затравки.
- Teardown рендер-потока/зависшей вкладки — всегда detach, не join.
- `/crush-fallback` остаётся в состоянии `armed` (не `active`) — `/crush` используется как обычно, переключение на `sh` только при реальном триггере.

## Open questions
- С какой из задач #218-226 начинать — пользователь спросил встречный вопрос, сессия прервана раньше ответа.
- Не выяснено, нужно ли коммитить сейчас остальные давно висящие незакоммиченные изменения рабочего дерева (compression removal, Hebrew niqqud/word-order fixes, renderstate.rs unsafe-rewrite, quad.rs/box_model.rs/paint.rs/pane.rs/background.rs из render-thread-подготовки задачи №201) — они не связаны с текущими двумя инициативами и лежат нетронутыми с ранних частей сессии.
- Untracked scratch-файлы в корне репо (`clippy_full3.txt`, `clippy_output.txt`, `clippy_output2.txt`, `clippy_targeted.txt`, `metadata.json`, `touched.tmp`, `word_order_test.txt`, `OnlyTerm-Setup.exe`) — не убирались, безопасны для удаления при первой возможности.
- Ни один коммит этой сессии не запушен.

## Repo state
```
 M Cargo.lock
 M Cargo.toml
 M crates/codec/Cargo.toml
 M crates/codec/src/lib.rs
 M crates/term/src/terminalstate/performer.rs
 M crates/term/src/test/mod.rs
 M crates/wezterm-font/src/shaper/rustybuzz.rs
 M crates/wezterm-gui/Cargo.toml
 M crates/wezterm-gui/src/quad.rs
 M crates/wezterm-gui/src/renderstate.rs
 M crates/wezterm-gui/src/termwindow/background.rs
 M crates/wezterm-gui/src/termwindow/box_model.rs
 M crates/wezterm-gui/src/termwindow/render/paint.rs
 M crates/wezterm-gui/src/termwindow/render/pane.rs
 M crates/wezterm-surface/src/cellcluster.rs
?? .claude/
?? OnlyTerm-Setup.exe
?? clippy_full3.txt
?? clippy_output.txt
?? clippy_output2.txt
?? clippy_targeted.txt
?? metadata.json
?? scripts/
?? touched.tmp
?? word_order_test.txt
```
```
6d49b04f9 docs: changelog note on review-driven fixes, execution-decoupling plan
d5d60d329 term: fix unused_io_amount (partial writes to pty) found by @oh re-review
f4e2d9268 vtparse, window, procinfo, env-bootstrap: fix misleading SAFETY comments (task #212/#217)
2dd032a24 window, wezterm-gui, mux, termwiz, wezterm-font, wezterm-dynamic, wezterm-input-types: fix findings from independent @oh review (task #212/#216)
b77a81899 color-types: fix NaN falling through the srgb8 clamp, add regression test
```
Ни один коммит не запушен. Остальные показанные в `git status` изменения (compression removal, Hebrew niqqud/word-order fixes, renderstate.rs unsafe-rewrite) — более раннее незакоммиченное состояние сессии, предшествующее unsafe-аудиту, всё ещё не запрошено к коммиту явно.
