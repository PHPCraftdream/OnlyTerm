# Checkpoint — 2026-09-06 14:38 [post-babygoal-render-fixes, gap-since]

## Session summary

This session (as far as I have direct memory of it) ended on 2026-09-02 at commit
`c9b39b951` after landing the `/babygoal … /wrush` render/resource bug-fix batch (see
`docs/checkpoints/2026-09-02-0721.md` for that session's full narrative: host-process
GPU-backend frame-loss lost-wakeup fix, torn-terminal-snapshot fix, CPU-waste fixes,
per-pane memory-leak fix — four tasks, #660–#663, all completed and merged to `main`).
That session's last actions: bumped the version tag to `v0.0.18-alpha`, added changelog
entries, pushed, caught and fixed a real `cargo +nightly fmt` failure the local (stable)
fmt checks had been silently missing all along (`imports_granularity = Module` needs
nightly), moved the tag to the fixed commit, and confirmed `fmt`/`windows_continuous`/
`windows_tag` all green.

**Today's date jumped from 2026-09-02 to 2026-09-06 with no session activity from me in
between** — `git log` shows 8 more commits landed on `main` after mine
(`09ae41e41`…`75fa8bb19`, all dated 2026-09-05/09-06), including a new tag
`v0.0.19-alpha`, e.g. "fix(input): use fresh process names for keyboard compatibility",
"perf(procinfo): avoid redundant process tree work", "perf(gpu): reuse host instance
buffers", "perf(gui): make logging and image decode responsive", "fix: stabilize startup
tab titles and use repository-local build output", "feat: make process title updates
opt-in" (this last one matches the `allow_process_title_updates` config option I can see
referenced in `docs/config/reference/config/default_tab_title.md`'s current on-disk
content). **I have no context on any of this work** — it was not done by me/this
conversation, I have not reviewed the diffs, and I don't know who/what produced it (another
session, the user directly, or a different agent). Several files were also flagged by the
harness as "changed on disk since you last read them": both `CLAUDE.md` files (project and
global — the project one gained a `CARGO_TARGET_DIR`-unset instruction and a
"run sub-agents in their own worktree, verify, merge, then delete it" rule; the global one
gained new critical prohibitions: never write private local paths into a repo, never load
the machine with fake workload without explicit request), `mkdocs.yml` (regenerated,
picked up a new `allow_process_title_updates` reference page), and
`default_tab_title.md` (rewording, presumably from the opt-in-titles change above).

Working tree is currently clean (`git status --short` — empty) at `75fa8bb19`. TaskList is
unchanged from the last session: #660–#663 completed, #599 (the standing "always launch
test builds with this cwd" note) still pending and not real work.

## Active goal

None set via `/goal`.

## TaskList

### pending
- #599 Always launch OnlyTerm test builds from the repository workspace directory
  (standing note, not executable work)

### recently completed
- #660 Fix host-process GPU backend frame-loss bugs (lost-wakeup + 2 stuck-frame paths)
- #661 Fix transient render-correctness bugs (torn snapshot, duplicate retained line, resize race window)
- #662 Fix CPU-waste bugs (WM_PAINT busy-spin, wasted paint_pass, process-snapshot storm, misc timers)
- #663 Fix memory-growth bugs (per-pane state never cleared, atlas silent demotion)

## Decisions

(Carried over from the 2026-09-02 session — nothing new decided in this turn, which is
just this `/checkpoint` invocation itself.)
- Restored only the fork-authored top section of the deleted `docs/changelog.md`.
- Grouped the render/resource bug-hunt's 10 lettered findings into 4 tasks, fixed via
  true-parallel `/wrush` worktrees.
- Moved the `v0.0.18-alpha` tag after-the-fact once a real `nightly`-only fmt failure was
  found post-push, rather than leaving a tag pointing at a commit that fails CI.

## Open questions

- **What is `09ae41e41`…`75fa8bb19` (8 commits, `v0.0.19-alpha`)?** Landed after this
  session's own work with zero context carried into this conversation. Before making any
  further changes in this area (especially anything touching process-title handling,
  tab-title stabilization, GPU host buffer reuse, or logging/image decode responsiveness),
  worth reading those diffs and their own commit messages/CI status rather than assuming
  they're unrelated to whatever comes next.
- Do the CLAUDE.md changes noted above reflect a deliberate new policy (they read as one:
  more critical prohibitions, an explicit sub-agent worktree-lifecycle rule, and a
  `CARGO_TARGET_DIR`-hygiene instruction consistent with the fingerprint-churn issue from
  the 09-01/09-02 session) — nothing here contradicts prior instructions, so treating them
  as currently in force.

## Repo state

```
(working tree clean)
```
```
75fa8bb19 style: satisfy nightly rustfmt import grouping
bb250a021 feat: make process title updates opt-in
89b933b3a fix: stabilize startup tab titles and use repository-local build output
a93ebfd4c test: complete image fixtures and document verified integration
61d2f56d9 test: pin stale image retry deadline regression
```

Tags: `v0.0.19-alpha` (newest, not from this conversation), `v0.0.18-alpha` (from the
2026-09-02 session).
