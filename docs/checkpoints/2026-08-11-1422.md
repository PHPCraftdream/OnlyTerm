# Checkpoint — 2026-08-11 14:22 [review-fixes-482-489-and-alt-v-hotfix]

## Session summary

Continuation of OnlyTerm (Windows-only wezterm fork) work, opening from a `/resume` of the
2026-08-10-2355 checkpoint (which covered the GitHub fork migration, upstream history squash,
the win32-input-mode Ctrl-key regression fix, the full Windows-only `#[cfg(unix)]` sweep, and
the git-history asset purge — all already committed and pushed by the start of this window).

This window's arc: (1) The user reported a fresh live bug — a recurring "OnlyTerm is not
responding" toast. Launched `@oh` to investigate; root-caused it myself independently to a
false-positive in the GUI-thread watchdog (`crates/window/src/os/windows/watchdog.rs`): the
heartbeat only ticks once before the message loop parks in `wait_message()`
(`MsgWaitForMultipleObjects`, `INFINITE`), so a long-but-legitimate idle period (or the whole
process being suspended by OS sleep/hibernate) looked identical to a genuine hang. Removed the
toast (kept logging), added an idle-wait exemption (`begin_idle_wait`/`end_idle_wait` bracketing
`wait_message()`) and a suspend/resume re-baseline check, with a new regression test. Committed
as `4a96ad3c2`, tagged `v0.0.7-alpha`, pushed (all explicitly requested by the user).

(2) The user then asked me to read `docs/investigations/2026-08-11-new-commits-review.md` (an
existing code-review doc covering commits `d0d63d9e6..4a96ad3c21`, flagging 3 P1 issues and
several P2/minor issues in the WebGPU render path, mux notification dispatch, and hashing) and
"заведи таски по нему" — created tasks #482–489, one per review finding, ordered by the review's
own stated priority.

(3) The user then invoked `/babygoal` with an explicit directive: implement #482–489 via the
external `crush` CLI (a separate AI coding agent tool, `crush run --role smart --restrict-run`),
commit between each task, and after all are done: `/checkpoint`, update the changelog, commit all
`.md` files, and run an `@oh` review of the whole round. `/babygoal`'s contract was followed:
`/babysit` armed at the default 15m interval (cron `4cfe033a`, still active), tasks #490–493
created for the wrap-up chain (checkpoint → changelog → commit-md → `@oh` review), all chained
via `blockedBy` after #489.

(4) Mid-flight, the user reported a SECOND live bug (Alt+V broken on non-US keyboard layouts,
demanding "a good design, not a patch"). Created task #494, investigated directly (no sub-agent —
the user hadn't given explicit permission for one this time), and root-caused it to
`crates/window/src/os/windows/keyboard_layout.rs`'s `probe_dead_keys()`: it included plain
`Modifiers::ALT` in the set of modifier states probed for dead-key behavior, and on several
non-US layouts, `ToUnicode` answers the dead-key sentinel (-1) for a plain-Alt+letter chord as a
driver artifact — silently swallowing the keypress before it ever reaches the keybinding
pipeline. Fixed by excluding plain `ALT` from the probe (keeping `RIGHT_ALT`/AltGr, which is the
only modifier state that legitimately composes text on any Windows layout), extracted into
`dead_key_probe_modifiers()`, locked in with a regression test. Committed `73ee22826`.

(5) Executed #482–489 via `crush`, one at a time in the `blockedBy` chain, each followed by my own
independent build/clippy(`-D warnings`)/fmt/test verification and a commit — the zero-trust
pattern already established earlier in the broader session. Results: `crush` produced good,
directly-committable diffs for #482 (async WebGPU init), #483 (instance-buffer race fix via
backpressure check before buffer write), #484 (device-lost context versioning/invalidation), and
#487 (AHash → `ahash::RandomState`). It produced a **broken no-op fix for #485** (added a
recursion-depth counter whose result was never branched on — the actual recursive call was
untouched); I reverted it and reimplemented directly (converted `Mux::notify`'s reschedule path
from a recursive `dispatch_notification` call into an internal bounded loop +
`promise::spawn::spawn_into_main_thread` yield past `PANE_OUTPUT_MAX_SYNC_ROUNDS = 64`
consecutive rounds, with a new test proving >1 but <N scheduler-queue hops for sustained output).
For #486, `crush`'s first attempt made zero changes (session ended after research, no edits); a
retry produced a correct core fix (`is_current: Arc<AtomicBool>` → `Weak<AtomicBool>`,
matching the field's own pre-existing-but-unimplemented doc comment) which I accepted and
extended with an extracted `subscriber_is_alive()` predicate + unit tests (the crush session
again didn't finish its own verification). For #488 and #489 (mechanical/trivial: `#[ignore]` on
two slow benchmark tests; log instead of `.ok()` a startup-command send error), `crush` produced
zero changes both times (or on #488's single attempt) — implemented both directly myself.

A **recurring, reproducible `crush` instability** was observed across this session: at least 5 of
7 `crush run` invocations had their session end abruptly with an empty final assistant message
right after issuing (or attempting to issue) a bash tool call — independent of `--allow-bash`
allowlist coverage, independent of `--quiet`. Adjusted later prompts to explicitly withhold
`cargo build`/`clippy`/`test`/`fmt` permission from `crush` and told it not to attempt
verification itself, which avoided the failure mode for #487 (no bash call attempted → no
stall) but did NOT prevent zero-change stalls on #486 (first attempt) / #488 (mechanical tasks).
Net effect: `crush` was net-useful for the four substantive P1 GPU/threading fixes (#482–484,
487) where it produced strong first-draft diffs I verified/extended, but unreliable/zero-value
for #485, #486 (partially), #488, #489, which I ended up implementing directly. All 8 tasks are
now complete and committed as 8 separate commits (`5ca562a70` … `862fab1b2`), each independently
verified via `cargo build`/`clippy --all-targets -D warnings`/`fmt --check`/`test` for the
affected crate(s) — nothing pushed yet (never explicitly requested this session).

Currently executing task #490 (`/checkpoint`, this file) — the wrap-up chain continues with #491
(update `docs/changelog.md`), #492 (commit all `.md` artifacts), #493 (`@oh` review of the whole
round), in that `blockedBy` order.

## Active goal

None (`/goal` not used this session; `/babygoal`'s TaskList-driven tracking is what's live —
see TaskList below and the still-armed `/babysit` cron `4cfe033a`, 15-minute interval).

## TaskList

### in_progress
- #490 Финальный /checkpoint после завершения задач ревью #482-489

### pending
- #491 Обновить docs/changelog.md по итогам задач ревью #482-489 (blockedBy: #490)
- #492 Закоммитить все .md артефакты сессии (checkpoint/changelog) (blockedBy: #491)
- #493 Ревью @oh: проверить всю работу по задачам #482-489 (blockedBy: #492)

### recently completed
- #489 Ошибки отправки startup-команд молча отбрасываются через .ok()
- #488 Вынести длительные бенчмарки из обычного тестового набора
- #487 AHash с фиксированными ключами для терминальных данных (потенциальный CPU DoS)
- #486 Подписчики device-lost накапливаются после закрытия окон (утечка)
- #485 Рекурсивный PaneOutput dispatch может монополизировать event loop
- #484 Восстановление после device lost повторно использует потерянный Device
- #483 Гонка данных в instance buffer между GUI- и рендер-потоком
- #482 Убрать блокирующее ожидание WebGPU-init с GUI-потока
- #494 СРОЧНО: Alt+V не работает на не-US раскладках — нужен принципиальный фикс
- (task #478-#481 and earlier, from the prior checkpoint's window, also completed — see
  `2026-08-10-2355.md` for that history)

## Decisions

- Removed the GUI-hang toast entirely (kept the underlying watchdog logging) per explicit user
  request ("тост давай уберем"), then implemented the two false-positive-elimination mechanisms
  (idle-wait exemption, suspend/resume re-baseline) per a follow-up explicit request, rather than
  just tuning the existing threshold (which wouldn't have helped — the false positives were 65s
  and 27h long, not borderline-4s cases).
- For Alt+V: fixed the *design* (excluded plain ALT from dead-key probing entirely, a
  layout-independent-by-construction fix) rather than patching the specific reported layout/key,
  per the user's explicit demand for a principled fix that can't recur for a different
  key/layout combination.
- Delegated #482–489 to `crush` per explicit user instruction, but treated every `crush` diff
  with the same zero-trust independent-verification discipline established earlier in the
  broader session (never trusted "worker reports done"; caught and reverted one genuinely broken
  no-op fix for #485 by re-reading the diff against the actual bug mechanism).
- Restricted every `crush` invocation (`--restrict-run` + a narrow `--allow-tool`/`--allow-bash`
  allowlist) rather than using crush's default auto-approve-everything mode, given the documented
  prior incident (a different delegated crush session once killed 4 live OnlyTerm sessions via
  `taskkill`) — git commit/push and any process-kill capability were never in the allowlist for
  any crush invocation this session.
- After observing `crush`'s repeated stall-after-bash-call failure mode, switched later task
  prompts (#487 onward) to explicitly withhold cargo-command permission from crush and do 100%
  of verification myself, rather than continuing to retry the same failure pattern.

## Open questions

- None the user hasn't already answered for this window's scope. Whether/when to push the 8 new
  commits (`5ca562a70`…`862fab1b2`) plus the earlier watchdog (`4a96ad3c2`) and Alt+V (`73ee22826`)
  commits is open — not requested yet this window; user pushed explicitly in the prior window
  each time, likely wants the same pattern here.
- The `crush` bash-call-stall instability (5+/7 sessions) is worth flagging to the user as a tool
  reliability issue, independent of this task round — not something I can fix from this session.

## Repo state

```
?? .claude/
?? ci/dev-install.log
?? docs/checkpoints/2026-08-10-1942.md
?? docs/checkpoints/2026-08-10-2355.md
?? docs/investigations/2026-08-11-new-commits-review.md
```
(docs/checkpoints/2026-08-11-1422.md, this file, not yet shown in the status snapshot above since
it was captured immediately before writing this file.)

```
862fab1b2 gui: log --start-conf startup-command send failures instead of dropping them
50643a14e gui: mark two benchmark tests #[ignore] so they don't slow cargo test
6e8373d2a gui: use ahash::RandomState instead of fixed-key AHasher for glyph caches
5236d5cb4 gui: prune device-lost subscribers whose owner window is gone
885a10cc2 mux: bound PaneOutput redelivery to a loop, yield to the event loop
47f08f8fd gui: invalidate the cached process-wide GPU context on device loss
3696e1533 gui: fix instance-buffer data race between GUI and render thread
5ca562a70 gui: make WebGPU process-context init non-blocking on the GUI thread
73ee22826 window: never probe plain Alt as a dead-key trigger on Windows
4a96ad3c2 gui,window: fix GUI-watchdog false positives, drop the hang toast
96d074daa crates: fix clippy findings surfaced by the Windows-only cleanup
6f2a47680 crates: remove non-Windows cfg branches (mux/config/termwiz/window/etc)
```

`main` is 10 commits ahead of what was pushed as of the last push (tag `v0.0.7-alpha` at
`4a96ad3c2`) — nothing pushed yet this window beyond that tag. `ci/dev-install.log` remains
untracked/locked (longstanding, low-priority, unrelated to this session).
