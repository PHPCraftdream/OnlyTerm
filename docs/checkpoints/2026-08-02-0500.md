# Checkpoint — 2026-08-02 05:00 [rhai-removal + WebGpu-hardening initiative fully complete; @oh review next]

## Session summary

Direct continuation of checkpoint `2026-07-31-1745.md`. Since that checkpoint: the 5th `/crush` attempt on task #276 finally succeeded (new crush build 0.1.7@0.87.0), but per user instruction ("удали все кроны, встаём на паузу") work was paused and all crons deleted before independent verification. The user then re-invoked `/babygoal` with the same standing contract (implement remaining tasks via `@sh`, commit between tasks, then checkpoint/changelog/review-with-`@oh` once everything is done). Delegation switched back from `/crush` to `@sh` for the rest of the session, per this explicit re-invocation.

Task #276 was independently verified (build/test/clippy all clean, 82→67 test count explained by the debug-REPL tests removed) and accepted. Task #277 (delete `lua-api-crates/*`) went through two @sh rounds: the first stopped correctly upon discovering 3 of 13 crates (`mux-lua`, `termwiz-funcs`, `color-funcs`) are live native dependencies, not dead code; a narrower re-scoped run then deleted the 11 genuinely-dead crates cleanly. Task #278 (delete `config`'s rhai engine) surfaced, across three separate stop-and-report rounds, that the rhai event-callback *bridge* itself (not just its config-loading and wezterm-gui-registration surface) was still live in far more places than originally scoped — this triggered creating two new tasks (#289 wezterm-gui remaining call sites, #290 mux/wezterm/wezterm-mux-server remaining call sites) which were completed first; #278 was then resumed three times in place (via `SendMessage` to the same agent) as it kept finding one more orphaned macro-invocation call site at a time (`color-funcs`, then `termwiz-funcs`, then the `rhai_config_smoke.rs` test file) — each was approved and folded in rather than spawning new tasks, since they were small and directly blocking #278's own deletion. Task #279 (remove the `rhai` crate from the workspace entirely) then landed cleanly, confirmed via `cargo tree -i rhai` erroring "did not match any packages". Task #280 (delete obsolete rhai-bridge test files) was equally clean, since #275 had already added the ktav-loading smoke tests that make the old rhai smoke test redundant. Task #281 (update all documentation for ktav) was the largest single sub-agent run of the whole initiative: it hit an "API Error: Connection closed mid-response" partway through (a transient infrastructure error, not a policy/content issue — confirmed no destructive state, just an interrupted turn), and was resumed twice via `SendMessage` to finish ~434 files (255 got a "Removed: no scripting engine" banner, ~180 got mechanical rhai→ktav syntax conversion, `migration-lua-to-rhai.md` was replaced by `migration-to-ktav.md`, `changelog.md` got the master entry).

With the rhai-removal initiative (#275-281, #289-290 — 9 tasks total) fully closed, work proceeded through the independent WebGpu/hang-recovery hardening backlog (#282-288, all originally surfaced by an `@oh` re-investigation of deferred findings from the tab-isolation initiative): #282 (null `webgpu_child_hwnd` on failed recreate), #283 (defer child-HWND destruction via `Weak<WebGpuState>` until the old renderer state actually drops — a genuine architectural change spanning `crates/window` and `crates/wezterm-gui`), #284 (hoist `create_uniform` out of the per-draw loop), #285 (instrument per-frame vertex-buffer churn with two new metrics histograms), #286 (make the 3-slot vertex-buffer rotation backend-dependent: 1 slot for WebGpu, 3 for Glium, reclaiming several MB per window), #287 (make the render-thread hang-check timer chain structurally single-instance via a `Cell<bool>` guard flag, closing a race that could otherwise let two supervisor chains run in parallel), and #288 (a bundle of four independent comment/doc accuracy fixes — `WriterWrapper`'s self-contradictory rationale, a "33x" vs "34x" measurement-table typo, an incorrectly Windows-scoped changelog line, and a doc-syntax check that turned out already resolved by #281).

Every single task in this entire arc (16 tasks: #275-290) was independently zero-trust-verified by me before being marked completed — re-running the actual build/test/clippy commands myself (not trusting the sub-agent's self-report), and in several cases reading the full `git show` diff to confirm scope matched the claim. No fabricated or rubber-stamped completions. After #288 closed, `TaskList` legitimately reported empty (all 17 tasks in the list — including the two carried over from before, #274 and the two housekeeping items — resolved), which is expected/correct behavior for a fully-drained list, not a data-loss bug (briefly worried about this until confirmed via git log that all commits were intact).

Per the standing `/babygoal` contract's final step, I then: added a changelog entry (`docs/changelog.md`) documenting the WebGpu-rebuild hardening bundle (#282-287) in the same voice/detail level as the existing hang-recovery changelog entries; committed all outstanding markdown (the changelog edit plus the previous session's `docs/checkpoints/2026-07-31-1745.md`, which had been sitting untracked) as commit `179d87626`; and am now writing this checkpoint. The final remaining step of the standing contract — dispatching an `@oh` review agent to audit the entire body of work — has not yet been done as of this checkpoint; it is the very next action.

## Active goal

A session-scoped Stop hook is armed with condition (verbatim): "реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу". Status: task implementation (✅ done, 16/16 tasks #275-290), commits between tasks (✅ done, one commit per task, all unpushed), `/checkpoint` (✅ this file), changelog update (✅ commit `179d87626`), commit all `.md` (✅ same commit), `@oh` review (❌ **not yet done — this is what unblocks the Stop hook**).

## TaskList

Empty — every task this session tracked (#274-290) is `completed`. No `pending`/`in_progress`/`blocked` items remain. (The babysit cron that was tracking this list auto-deletes itself once it observes this state; it may already be gone or may delete itself on its next fire.)

## Decisions

- Fold small, directly-blocking follow-on discoveries into the *same* task via `SendMessage`-resume rather than spawning a new TaskList task, when the discovery is small and squarely inside the original task's own cleanup radius (e.g. #278's three rounds of "one more orphaned macro invocation" — color-funcs, termwiz-funcs, `rhai_config_smoke.rs`). Reserve new TaskList tasks for discoveries that are structurally distinct or too large to fold in without derailing the original task's scope (e.g. #289/#290, which were carved out of #278 because the missing call sites spanned entire other crates, not a one-line fix).
- An "API Error: Connection closed mid-response" from a sub-agent is treated as transient infrastructure failure, not a signal to abandon or restart from scratch — checked `git status` first to confirm no partial/corrupt state, then resumed the *same* agent via `SendMessage` (which retains its own context of what it already did) rather than launching a fresh one that would have to rediscover everything.
- `TaskList` reporting "No tasks found" after the last task completes is expected/correct behavior for a fully-drained list, not data loss — verified via `git log`/`git status` that all underlying work was intact before concluding this, rather than assuming and moving on.
- Every sub-agent's self-reported "verified" claim was independently re-run (build/test/clippy, and for larger changes a full `git show` diff read) before accepting a task as done — zero-trust discipline maintained across all 16 tasks in this arc, matching the standard established earlier in the session for `/crush` output and carried over identically to `@sh` output.

## Open questions

- None blocking. The only remaining step is the `@oh` review the standing goal requires, which is the immediate next action after this checkpoint.
- Long-standing, still open: whether/when to push the ~20 unpushed commits accumulated since the last push (`67c17cb04..3f18c5f29`) — no push instruction has been given since that last explicit "сделай коммиты и пуш"; per standing CLAUDE.md constraints, still awaiting an explicit push request.

## Repo state
```
?? .claude/
?? OnlyTerm-Setup.exe
?? clippy_full3.txt
?? clippy_output.txt
?? clippy_output2.txt
?? clippy_targeted.txt
?? metadata.json
?? touched.tmp
```
(Working tree otherwise clean. All 17 task-related commits below are unpushed.)
```
179d87626 docs: changelog entry for WebGpu rebuild hardening (tasks #282-287), checkpoint
2cfeae61b mux, docs: small comment/doc accuracy fixes -- WriterWrapper rationale, 33x->34x typo, changelog Windows-scoping (task #288)
dca7477b2 wezterm-gui: make the render-thread hang-check timer chain structurally single-instance via a guard flag (task #287)
91ede2418 wezterm-gui: make vertex-buffer rotation slot count backend-dependent (1 for WebGpu, 3 for Glium) (task #286)
80ef5807e wezterm-gui: instrument per-frame WebGpu vertex-buffer recreate+unmap churn (task #285)
```
