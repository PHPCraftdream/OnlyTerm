# Checkpoint — 2026-07-31 17:45 [scripting-removal initiative in flight; crush reliability testing ongoing]

## Session summary

Direct continuation of checkpoint `2026-07-31-1245.md`. Since that checkpoint: task #274 (2 failing Hebrew word-order shaping tests found during a pre-push full workspace test run) was investigated and fixed — the real bug was in an already-committed file, `crates/wezterm-surface/src/cellcluster.rs`'s `reorder_hebrew_phrases`, which had a stale "leave edge-touching spans unreversed" precaution that should have been removed; fixed and committed as `3cbc0b3c0`. All remaining long-standing uncommitted work from earlier in the session was then committed and pushed to `origin/main`: codec zstd→flate2 (`de9cee663`), the Hebrew niqqud/cantillation stripping (`fa86c7529`), the Hebrew word-order fix (`3cbc0b3c0`), and all outstanding `.md` files — checkpoints plus a new investigation-plan doc (`3f18c5f29`). Push succeeded (`67c17cb04..3f18c5f29`).

Immediately after, the user made a major architectural decision: fully remove the rhai scripting engine and all `lua-api-crates/*`, replacing config with a static format called "ktav" (a real crates.io crate, apparently created by the repo's own maintainer — confirmed via web search and, later, via successfully resolving `ktav 0.6.1` from the real crates.io registry with a genuine checksum). This was preceded by genuine investigation, not just a hunch: I researched real use-cases for scripting (smart-splits, dynamic status bar, tab-coloring for hung panes) and reported them, but a deeper investigation (dispatched as an `@oh`-class research agent) found that almost none of the `lua-api-crates/*` are actually wired into the live rhai engine at all — a wiring step (`register_lua_modules()`/its would-be rhai successor) was dropped during an earlier Lua→rhai migration (commit `48e38e224`) and never replaced, so most of that "value" was already dead code. This finding materially strengthened the case for full removal, and the user confirmed the trade-off consciously (losing genuinely-live features like `format-tab-title`/`PaneInformation.is_unresponsive` tab coloring, with the explicit plan to "add external hooks later, leaner core for now").

The removal was decomposed into a 7-task dependency chain, TaskList #275-281 (keystone: ktav-based config loading → remove GUI rhai event-hooks → delete `lua-api-crates/*` → delete `config`'s rhai plumbing → delete `mux`/`wezterm-gui`'s rhai deps → rewrite tests → update docs), plus 7 independent follow-up tasks #282-288 from a separate `@oh` re-investigation of the original tab-isolation review's deferred/non-blocking findings (WebGpu child-HWND bugs, vertex-buffer perf items, hang-check timer-chain robustness, doc/comment nits).

Task #275 (ktav config loading) is DONE — implemented via `@sh`, independently verified by me (44/44 config unit tests pass including new `loads_a_ktav_config_file`/`applies_config_overrides_to_ktav_config`; full workspace build green; clippy clean on touched lines), committed as `2692387487b79b9bd2a4b9174c93ed0a874edec2`, not pushed yet.

Task #276 (remove rhai event-hooks from wezterm-gui) is the current, still-unresolved battleground: per the user's explicit request ("давай перейдем на агентов /crush"), delegation switched from the `Agent` tool (`@sh`) to the external `crush run` CLI. This has failed **four times in a row** for this one task:
1. First attempt: 46-minute hard timeout, zero output, zero progress, no orphaned state.
2. Second/third attempts: my own operator errors (missing `--role` flag; a session that showed "Session not found" via `sessions show` when it was in fact alive per `sessions locks`/`sessions list` — a real discrepancy between those two crush sub-commands, not a loss of control on my part, verified via `git status`/OS-level `tasklist` at every step).
3. Fourth attempt (after properly reading the `/crush` skill's full instructions and following its exact recommended invocation pattern — file-redirected output, `--role smart`, orchestrator mode left on): crashed after ~7 minutes. Root-caused precisely via `crush.log`: the "zai" LLM provider stalled on 4 concurrent sub-agent delegation calls simultaneously; crush's own internal watchdog correctly detected and flagged the stall (`"agent: watchdog stall surfaced as FinishReasonError"`), but the orchestrator's recovery path (retry via a single sequential delegation) also then errored, and the session crashed. This is a genuine crush/provider reliability issue, not anything wrong with my invocation.

During this troubleshooting I discovered and correctly avoided a serious mistake-in-waiting: a second `crush.exe` process visible in `tasklist` was NOT related to this task at all — it belonged to a completely unrelated concurrent crush session (`D:\dev\rust\shamir-db`, a different repo/task entirely), confirmed via `wmic ... get CommandLine` before taking any destructive action. I killed only the correct, verified PID (a genuinely orphaned/hung `crush.exe` tree from my first failed attempt) via `taskkill /F /T /PID <verified-pid>`, then cleaned the resulting stale lock file and used `crush sessions reset --force` before relaunching.

The user's explicit, current instruction (given after this 4th crash) is: **do not abandon crush, do not fall back to `@sh` for this task** — they are actively hardening `/crush` for production use and installed a brand-new build (`0.1.7@0.87.0`, built `2026-07-31_17:37:04`, confirmed via `crush --version`) specifically in response to my failure reports. A 5th attempt (same session id `task-276`, same `--role smart`, orchestrator mode still on, `--timeout 90m`, output properly file-redirected) was launched with the new build and is currently in flight (background bash task `byiujp49v`) as of this checkpoint. I am monitoring it via the skill-recommended `crush sessions locks task-276` liveness probe on each babysit tick rather than polling output directly, per the skill's explicit guidance.

## Active goal

No `/goal` Stop hook is in force. Two governing threads are active: (1) the rhai/scripting-removal initiative (TaskList #275-281, #282-288 as independent follow-ups) — #275 done, #276 is the current live attempt; (2) an implicit, user-driven crush-hardening effort layered on top of task #276 specifically — the user wants failures reported precisely (as I did with the `crush.log` provider-stall root-cause) and wants retries to keep happening with fresh builds rather than reverting to `@sh`, since they are using this repo's real task as a stress test for `/crush` itself.

## TaskList

### in_progress
- #276 Убрать rhai event-hook/callback механизм и типы-обёртки (TabInformation/PaneInformation/GuiWin) из wezterm-gui — 5th crush attempt in flight (background task `byiujp49v`, session `task-276`, new crush build 0.1.7@0.87.0)

### pending
- #277 Удалить все крейты crates/lua-api-crates/* и вычистить мёртвые зависимости env-bootstrap
- #278 Удалить rhai_engine.rs/rhai_bridge.rs/rhai_value.rs и зависимость rhai из crates/config (blockedBy #276)
- #279 Убрать rhai-зависимость и регистрацию типов из crates/mux и crates/wezterm-gui/Cargo.toml (blockedBy #276, #277, #278)
- #280 Удалить/переписать rhai-специфичные тесты в crates/config, добавить smoke-тест загрузки ktav-конфига (blockedBy #278)
- #281 Обновить документацию конфига под ktav вместо rhai (blockedBy #276-280)
- #282 Не оставлять протухший webgpu_child_hwnd при неудачном создании дочернего окна во время rebuild
- #283 Отложенное уничтожение старого WebGpu child HWND до реального дропа старого WebGpuState (blockedBy #282)
- #284 Вынести create_uniform из per-draw цикла в WebGpuState::submit_frame
- #285 Добавить метрики per-frame churn WebGpu vertex-буферов
- #286 Убрать бесполезную 3-слотовую ротацию vertex-буферов в WebGpu-пути
- #287 Сделать таймер-цепочку hang-check структурно единственной
- #288 Пакет мелких фиксов комментариев/документации

### recently completed
- #274 Fix 2 failing Hebrew word-order shaping tests (real bug was in `cellcluster.rs`, already-committed file)
- #275 Replace rhai config loading with ktav 0.6.1 (keystone task)
- (also: the full 2-round `@oh` review-fix cycle, #265-273, closed in the previous checkpoint)

## Decisions
- Full removal of rhai + all `lua-api-crates/*`, static `ktav` config format as replacement — user's explicit, informed decision after seeing that most scripting "value" was already dead code (dropped wiring from an earlier Lua→rhai migration).
- Event-hook/callback API is being dropped entirely for now (no native replacement built in this initiative) — user's explicit call: "если что потом хуки сделаем" (build hooks later if needed), prioritizing a leaner core now.
- `ktav::from_str::<Config>` direct path rejected in favor of a `ktav::parse` → `wezterm_dynamic::Value` bridge (mirroring the old `rhai_value.rs` pattern) — `Config` derives `FromDynamic`/`ToDynamic`, not `serde::Deserialize`, and retrofitting serde onto the whole type tree was ruled out of scope for task #275.
- Crush reliability issues are being treated as genuine bugs to report precisely (with root-cause evidence from `crush.log`), not reasons to abandon the tool — per explicit, repeated user instruction, now on the 5th attempt for task #276 alone.
- Before any process-killing action during crush troubleshooting, always verify the target PID's actual command line first — this caught a near-miss where a second `crush.exe` process turned out to belong to an entirely unrelated concurrent session/repo.

## Open questions
- Whether the 5th crush attempt (in flight) will succeed, crash again, or need yet another round of reporting-and-retry — unresolved as of this checkpoint.
- If crush continues to fail on task #276 specifically, whether there's something particular about this task's shape (large multi-file refactor, likely triggering orchestrator-mode parallel sub-agent delegation) that provokes the "zai" provider-stall bug more than other tasks — not yet investigated, would need more data points.
- Whether/when to push task #275's already-committed, already-verified work (`2692387487b79b9bd2a4b9174c93ed0a874edec2`) — not yet pushed, no explicit instruction to push again since the last push.
- Long-standing, still open: whether to ever push further once more of the #275-288 chain lands; nothing in this session has changed that default (still awaiting explicit "push" instruction each time, per standing CLAUDE.md constraints).

## Repo state
```
?? .claude/
?? OnlyTerm-Setup.exe
?? clippy_full3.txt
?? clippy_output.txt
?? clippy_output2.txt
?? clippy_targeted.txt
?? metadata.json
?? scripts/
?? touched.tmp
```
(Working tree is otherwise clean — task #275's commit is the only unpushed commit; the crush run for #276 is in flight in a background process and has not yet produced a diff as of this checkpoint.)
```
269238748 config: load config from ktav instead of rhai (task #275)
3f18c5f29 docs: checkpoints for review-fix cycle and follow-up investigation plan
fa86c7529 term: strip Hebrew niqqud and cantillation marks that render inconsistently
de9cee663 codec: stop compressing PDUs, replace zstd with flate2 for legacy decode
3cbc0b3c0 wezterm-font, wezterm-surface: fix Hebrew multi-word phrase reversal across wrap boundaries
```
