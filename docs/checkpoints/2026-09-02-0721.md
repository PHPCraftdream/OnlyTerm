# Checkpoint — 2026-09-02 07:21 [babygoal-render-fixes-shipped]

## Session summary

Continuation from checkpoint `2026-08-24-1656.md` (rebrand shipped, CI green). This session
had four threads, landing in this order.

**Thread 1 — CI action-version hygiene.** User pointed at "several things failing in CI
logs". Bumped `actions/cache`→v5, `mozilla-actions/sccache-action`→v0.0.11 (both were
targeting the deprecated Node 20 runtime) in `ci/generate-workflows.py`, committed
(`22ce559ac`). Rebased and merged the two open Dependabot PRs (#7 `cargo-install`, #8
`checkout`) on top, syncing the generator's own hardcoded pins so regeneration wouldn't
revert them; merged both (`38cf592d8`, `43da373c0`).

**Thread 2 — `verify-pages` strict-mode failures (280 warnings), traced to their root
cause.** Deep-dived the actual failing CI log rather than guessing. Found: the rebrand's
deletion of `docs/changelog.md` wasn't just removing upstream's redundant history — its top
`### Continuous/Nightly` section was this fork's own actively-maintained changelog (ktav
migration, first WezTerm→OnlyTerm rename round, SSH removal, etc.), and 243 reference pages
link to it via `#continuousnightly`. Restored just that section (through the boundary with
the old upstream dated-release history, per explicit user choice via `AskUserQuestion`) from
`22d6ede40~1`. Also fixed two real pre-existing bugs found along the way: a stale
`Page("onlyterm ssh", "cli/ssh.md")` entry in `ci/generate-docs.py` (SSH client was removed
project-wide, this wasn't updated), and an off-by-one relative path in
`default_tab_title.md`. Committed as `e0f07e50a`.

**Thread 3 — small user-facing fixes, done directly (no delegation).**
- New-tab default keybinding: removed the explicit `Ctrl+T` base binding in
  `crates/onlyterm-gui/src/commands/derive.rs` (kept only `SUPER+T`, whose permutation
  already synthesizes `Ctrl+Shift+T` for Windows/Linux) — `Ctrl+T` commonly collides with
  shell/readline bindings. Added a regression test. Committed `8f867381d`.
- Explained (no code change) why copy-to-clipboard already correctly excludes real
  terminal-width soft-wraps from pasted newlines (`extract_selection_text`,
  `wrapped()` cell attribute) but *cannot* do anything about a CLI tool (Claude Code)
  manually word-wrapping its own output with a hanging indent before it ever reaches the
  terminal — those are indistinguishable "hard" newlines at the PTY byte level. Recommended
  against a code-fence-aware heuristic hack (real risk of corrupting copied code blocks).
- Scrollbar thumb invisible under alt-screen apps (reported after a Claude Code render-engine
  update): root cause was `scrollback_rows == viewport_rows` on alt-screen making the thumb
  render at full track height (visually indistinguishable from "no thumb"), plus a
  `0.0/0.0`-NaN division silently masked by Rust's saturating float→int cast. Fixed in
  `crates/onlyterm-gui/src/scrollbar.rs` + `termwindow/render/pane.rs` (hide the thumb
  entirely when there's nothing to scroll). Committed `fc3dcf1ee` (mid-babygoal, on explicit
  user prompt).

**Thread 4 — the render/resource bug hunt and its `/babygoal … /wrush` fix-out (dominates
this session).** Launched an `fh` (Fable, high effort) agent to hunt for render glitches and
CPU/GPU/memory waste, seeded with the "ghost cursor" history (`0c7fbab37`/`58097a015`
already-fixed defect + the still-open question of a second trigger path). It wrote
`docs/investigations/2026-08-25-render-and-resource-bug-hunt.md`: ranked findings A–J, with
**A** (a lost-wakeup regression in `HostProcessBackend`'s `in_flight`/`repaint_pending`
handshake — reimplemented with `Release`/`Acquire` instead of the `SeqCst` already proven
necessary for the sibling in-process render thread in `0e9807e0d`/`aaf1f8f58`) as the
leading candidate for the user's recurring "input box not on the prompt's line" symptom.
Also confirmed the abandoned local branch `fix/hostprocess-oom` was fully superseded by
`32b92900a` on `main` — deleted it (worktree + branch) on request.

Grouped the findings into four tasks (not 1:1 with the report — user explicitly asked for
grouping): **#660** (A/C/D, host-process frame-loss, highest priority), **#661** (B/E,
transient torn-snapshot + duplicate-retained-line), **#662** (F/G/H, CPU waste), **#663**
(I/§3.6, memory growth). Deleted the now-superseded original ghost-cursor task **#658**
(its investigation is what produced #660/#661).

Ran `/babygoal реши задачи параллельно с помощью /wrush`: armed a 15-minute `/babysit`
cron, then launched **4 concurrent `rush` sub-agents**, each in its own mandatory `/wrush`
git worktree (`worktrees/fix-hostprocess-frameloss`, `fix-render-tearing`,
`fix-render-cpu-waste`, `fix-render-memory-growth`), each told to use an isolated
`CARGO_TARGET_DIR` inside its own worktree (the shared `D:\dev\rust\.cargo-target` would
otherwise thrash under 4 concurrent builds — a known issue from earlier in the project's
history). `#599`'s pre-existing note about `.gitignore` needing a `/worktrees/` entry was
added as part of `/wrush`'s own mandatory one-time setup.

All four landed, each **independently zero-trust verified by me** (not trusting the rush
envelope): read every diff hunk personally, manually traced the trickier logic (e.g. hand-
verified all 6 `RetainedPaneRows::shift_origin` test cases' expected values, independently
re-derived the Release/Acquire-vs-SeqCst memory-ordering argument against the actual code),
re-ran `build`/`clippy --all-targets -D warnings`/`fmt --check`/`test` myself in each
worktree AND again on `main` after merging. Two crashes occurred along the way (#660 and
#661 both hit rush's internal 45-minute *per-tool-call* watchdog, not the overall
`--timeout`) — both were genuinely transient (confirmed via `rush sessions why` showing a
dead PID, not a live lock), resumed into the same session id each time with increasingly
specific nudges ("don't chain build+clippy+test in one call", "don't delegate the
verification phase to a sub-agent"); #660 needed three resume attempts total before it
finished cleanly.

Landing order: **#663** first (`1a1b6aab0`, clean cherry-pick) → **#662**
(`60e406aed`, auto-merged cleanly against #663's touch to `render_pipeline.rs` — different,
non-adjacent lines) → **#661** (`5417a8f10`, auto-merged against the earlier scrollbar fix
in the same file; found and fixed one real cross-branch defect myself before merging — a
stale trailing comment referencing the removed `stable_range` variable) → **#660**
(`31ad32ad8`, clean cherry-pick; this rush agent's own diff additionally caught and fixed
two more `Release`/`AcqRel` sites in `RenderThreadHandle::send_frame` that predated
`aaf1f8f58` and weren't part of its original fix). One genuine post-merge conflict
surfaced only by `clippy --all-targets` (not by git's line-based merge, since the two
diffs touched different files): a `#[cfg(test)]` helper in `render_pipeline.rs` (from
#663's branch) constructed a `RetainedPaneRows` literal missing the `viewport_top` field
`#661`'s branch had just added — fixed with a one-line, separately-committed patch
(`21771b838`).

Combined final state on `main`: all four `RenderBackend`/render-pipeline crates
(`onlyterm-gui`, `onlyterm-gpu-render`, `onlyterm-gui-render-thread`, `onlyterm-gpu-protocol`,
`mux`) build/clippy/fmt/test clean — 198 unit tests across the touched crates, 0 failures.
Babysit cron self-deleted (by me, manually, once the last task completed — left running it
would have mis-tried to "start work" on `#599`, which is a standing note not an executable
task).

Finally: built the new `main` with the `dev-install` cargo profile and launched it (cwd
`C:\Users\Computer\Desktop\pc`, per `#599`), for the user to try. Discovered mid-launch that
Git Bash's `$!` reported a PID (25629) that **did not correspond to any real process**
(`wmic` returned "No Instance(s) Available") — found the *actual* PID (**24804**, parent;
its own spawned `gpu-tab-host` child is **13044**) by cross-referencing `wmic process where
name='onlyterm-gui.exe'` output against the executable path and creation timestamp. Startup
log is clean (GPU init succeeded, first frame presented). This is a live process launched
*by this session* — track it as such; it is separate from the user's ~14 other long-running
`OnlyTerm` windows (all from `C:\Program Files\OnlyTerm\onlyterm-gui.exe`, not this
dev-install build) that must never be touched.

## Active goal

None set via `/goal` this session.

## TaskList

### pending
- #599 Always launch OnlyTerm test builds with cwd C:\Users\Computer\Desktop\pc
  (standing note, not executable work)

### recently completed
- #660 Fix host-process GPU backend frame-loss bugs (lost-wakeup + 2 stuck-frame paths)
- #661 Fix transient render-correctness bugs (torn snapshot, duplicate retained line, resize race window)
- #662 Fix CPU-waste bugs (WM_PAINT busy-spin, wasted paint_pass, process-snapshot storm, misc timers)
- #663 Fix memory-growth bugs (per-pane state never cleared, atlas silent demotion)

### deleted this session
- #658 (superseded — its investigation produced #660/#661)

## Decisions

- Restored only the fork-authored top section of the deleted `docs/changelog.md`, not the
  full file — recovers the 243 broken inbound links without reintroducing hundreds of
  pre-rebrand "WezTerm" mentions outside the README (user's own later rule).
- Scrollbar: hide the thumb entirely (not draw a full-height one) when
  `scrollback_rows <= viewport_rows` (alt-screen, or any "nothing to scroll" state) —
  matches common terminal UX rather than leaving a misleading full-bar render.
- Grouped the bug-hunt's 10 lettered findings into 4 tasks (not 1:1) per explicit user
  instruction, ranked by the report's own confidence/severity ratings.
- Ran the 4 fixes via mandatory-worktree `/wrush` in true parallel (not sequential) per
  explicit user instruction, accepting the (correctly anticipated and successfully
  resolved) risk of cross-branch merge conflicts in shared files.
- Each parallel rush agent got its own isolated `CARGO_TARGET_DIR` to avoid the
  fingerprint-churn/false-cache-miss issue observed earlier in this project's history from
  concurrent agents sharing one target dir.
- Manually stopped the babysit cron once the TaskList's real work was done, rather than
  letting it try to "execute" the standing note `#599` on its next tick.

## Open questions

- None blocking. Two small housekeeping items left uncommitted on `main` on purpose (not
  asked for): the `.gitignore` addition of `/worktrees/` (mandatory `/wrush` one-time setup)
  and the still-untracked `docs/investigations/2026-08-25-render-and-resource-bug-hunt.md`
  (copied into every worktree as reference material during the parallel run, never
  committed to `main` itself — only its *content*, via the fix commits' messages, references
  it).
- `worktrees/fix-render-tearing` is an empty, git-untracked leftover directory that Windows
  wouldn't release (no process found holding it via `wmic`) — harmless (gitignored), will
  likely resolve itself; not force-deleted.
- The §3.6 finding (silent, permanent loss of host-process crash isolation when the atlas
  mirror exceeds 128MiB, with zero user-facing signal) was deliberately left as a documented
  follow-up by the #663 agent, not fixed — noted in that task's own final report, not
  re-opened as a new task.

## Repo state

```
 M .gitignore
?? docs/investigations/2026-08-25-render-and-resource-bug-hunt.md
```
```
31ad32ad8 gui: fix host-process GPU backend frame-loss bugs (lost-wakeup + 2 stuck-frame paths)
21771b838 gui: fix RetainedPaneRows literal missed by cross-branch field addition
5417a8f10 gui, mux: fix torn terminal snapshot and duplicate retained line on scroll
60e406aed gui: fix CPU-waste bugs (WM_PAINT busy-spin, wasted paint_pass, process-snapshot storm)
1a1b6aab0 gui: clear per-pane caches when a pane is removed
```

Nothing pushed to `origin/main` this session (last push was the prior session's rebrand).
A live process launched by this session for manual testing is still running:
`onlyterm-gui.exe` PID **24804** (parent) + PID **13044** (its `gpu-tab-host` child),
built from this session's HEAD (`31ad32ad8`), cwd `C:\Users\Computer\Desktop\pc`.
