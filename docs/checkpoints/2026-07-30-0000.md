# Checkpoint — 2026-07-30 00:00 [execution-decoupling: tasks #218-220 done, pausing before #221]

## Session summary
Прямое продолжение чекпоинта `2026-07-29-2100.md`. Governing-инструкция:
`/babygoal` "реализуй задачи с помощью @sh, между тасками делай коммиты. После
завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и
запусти ревью агента @oh - пусть проверит всю работу" — применяется к задачам
**#218-226** (инициатива execution-decoupling).

Три задачи выполнены последовательно через `Agent(subagent_type="sh")`, с
независимой верификацией диффа/сборки/тестов перед каждым коммитом:

- **#218** (watchdog GUI-потока) — коммит `773aa2920`. Атомарный heartbeat-
  счётчик в `run_message_loop()`, фоновый поток детекции залипания,
  `gui_watchdog_enabled`/`gui_watchdog_threshold_ms` в конфиге,
  `gui_thread_is_hung()` для будущего потребителя (задача #223).
- **#219** (снимок строк рендера) — коммит `6aab9a859`. `paint_pane` теперь
  берёт строки через `Pane::get_lines` (короткая блокировка на клон) вместо
  `with_lines_mut` (держал `Mutex<Terminal>` на весь проход шейпинга).

При независимой проверке **#219 оказался сломан в изоляции**: агент
использовал `wezterm_surface::cellcluster::CellCluster::is_hebrew_cell`, но
`wezterm-surface` не был добавлен в зависимости `wezterm-gui`, а сам метод был
приватным — баг маскировался посторонним незакоммиченным изменением
`Cargo.toml`, уже висевшим в рабочем дереве с начала сессии. Исправлено
отдельным коммитом `ccdd782d1`.

- **#220** (lock-free `ui_items`) — при работе над ней обнаружилась вторая,
  более крупная проблема: `paint.rs`/`pane.rs` физически не собираются без
  ещё одного предсуществующего незакоммиченного рефакторинга
  (`with_quad_allocator`, устраняющий unsafe/transmute self-referential
  структуры в `renderstate.rs` — старая задача №201, не связанная ни с
  unsafe-аудитом, ни с текущей инициативой). Пользователь подтвердил
  ("нужно всё наладить") закоммитить это отдельно. В итоге получилось
  **три отдельных коммита**:
  - `ccdd782d1` — фикс регрессии #219 (wezterm-surface dependency + pub fn).
  - `ae14bad62` — завершение задачи №201 (`with_quad_allocator`); чистый
    рефакторинг без unsafe/transmute, `clippy -p wezterm-gui` warnings
    51→40 (строго меньше, ни одного нового).
  - `b49fdf6c4` — собственно задача #220: `ui_items_scratch` (обычный Vec,
    как раньше) + новое поле `ui_items: ArcSwap<Vec<UIItem>>`, публикуемое
    один раз в конце `paint_pass()`; единственный читатель
    (`resolve_ui_item` в mouseevent.rs) делает `.load()` без блокировок.

Каждый из 5 коммитов (#218, #219, fix, #201, #220) независимо проверен: сборка
+ тесты + clippy **в изоляции** (стэш всех остальных незакоммиченных
изменений, включая друг друга) — это было критично, так как именно неполная
изоляция дважды маскировала реальные дефекты (#219's missing dependency,
#220's скрытая связность с №201).

Чейнджлог обновлён (секция `#### New`, абзац про watchdog/lock-free
прогресс). Этот чекпоинт пишется как часть текущего шага перед тем, как
запросить у пользователя декомпозицию задачи #221 через `@oh`.

## Active goal
`/babygoal`-цикл для задач #218-226: "реализуй задачи с помощью @sh, между
тасками делай коммиты. После завершения всей работы сделай /checkpoint,
обнови чейнджлог, закомить все мд и запусти ревью агента @oh". Промежуточный
`/checkpoint` + обновление чейнджлога — этот шаг. Полное условие (все 9 задач
+ финальный `@oh`-ревью) ещё не выполнено.

## TaskList
### completed (эта сессия, execution-decoupling)
- #218 Watchdog GUI-потока — `773aa2920`
- #219 Снимок строк рендера — `6aab9a859` (+ фикс регрессии `ccdd782d1`)
- #220 Lock-free `ui_items` — `b49fdf6c4` (+ побочный коммит №201 `ae14bad62`)

### pending
- #221 Рендер-поток на окно (WebGPU): вынести GPU из message loop — **пользователь попросил сначала запросить у `@oh` декомпозицию этой (и, возможно, соседних) задачи на более мелкие подзадачи, прежде чем начинать реализацию**
- #222 Рендер-поток (OpenGL) (blockedBy #221)
- #223 Супервизия вкладок: kill+dump зависшей вкладки (blockedBy #218, #221)
- #224 Опция «окно = отдельный процесс»
- #225 Async-оценка pty reader+parser (blockedBy #219)
- #226 Регресс-тест изоляции (blockedBy #221, #223)
- #213 crush-fallback state (persistent) — STATUS: armed, AGENT: sh

## Decisions
- Три относительно небольшие задачи (#218-220) — делегированы `sh`-агенту
  последовательно, каждая с независимой верификацией и отдельным коммитом.
- Задача #221 качественно крупнее и рискованнее (архитектурное изменение
  пайплайна рендеринга) — пользователь решил НЕ делегировать её в том же
  темпе, а сначала попросить `@oh` декомпозировать её на более безопасные
  подзадачи.
- Обнаруженные по пути дефекты и связности (missing wezterm-surface
  dependency от #219; скрытая зависимость paint.rs/pane.rs от
  предсуществующего рефакторинга №201) исправлены отдельными,
  правильно атрибутированными коммитами, а не втихую подмешаны в
  коммиты #219/#220.
- Проверка "собирается ли коммит в изоляции" (git stash всего остального)
  стала обязательным шагом перед каждым коммитом в этой инициативе — дважды
  поймала реальные проблемы, которые обычная сборка (на грязном дереве) не
  показывала.

## Open questions
- Как декомпозировать задачу #221 — сейчас передаётся `@oh` на рассмотрение.
- Остаётся открытым (унаследовано от прошлого чекпоинта): нужно ли и когда
  коммитить остальные давно висящие несвязанные изменения рабочего дерева
  (codec/compression removal, term/performer.rs, wezterm-font/shaper,
  оставшаяся часть Hebrew word-order фикса в cellcluster.rs помимо уже
  закоммиченного `pub fn is_hebrew_cell`).
- Ни один коммит этой сессии не запушен.

## Repo state
```
 M Cargo.lock
 M Cargo.toml
 M crates/codec/Cargo.toml
 M crates/codec/src/lib.rs
 M crates/term/src/terminalstate/performer.rs
 M crates/term/src/test/mod.rs
 M crates/wezterm-font/src/shaper/rustybuzz.rs
?? .claude/
?? OnlyTerm-Setup.exe
?? clippy_full3.txt
?? clippy_output.txt
?? clippy_output2.txt
?? clippy_targeted.txt
?? metadata.json
?? scripts/
?? touched.tmp
?? word_order_test.txt
```
```
b49fdf6c4 wezterm-gui: lock-free ui_items publication
ae14bad62 wezterm-gui: replace transmute-based quad-buffer mapping with safe closures
ccdd782d1 wezterm-gui, wezterm-surface: fix missing dependency from commit 6aab9a859
6aab9a859 wezterm-gui: snapshot pane lines before shaping to shorten terminal lock hold
773aa2920 window: add GUI message-loop watchdog (heartbeat + hang detection)
```
