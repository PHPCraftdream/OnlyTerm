# Checkpoint — 2026-08-12 15:24 [oh2-cleanup-round-in-flight]

## Session summary

Continuation of a long-running OnlyTerm (Windows-only wezterm fork) session. This
window's arc: closed out the previous `/babygoal` round (tasks #532-536, upstream
cruft cleanup — vendoring wgpu-hal, removing dead crates/deps, fixing broken CI
references — all committed and merged before this checkpoint's scope). Then ran a
**second** `@oh`-agent audit specifically asking "what else can be deleted as
upstream cruft now that we're Windows-only" — that agent returned a much deeper
sweep (§A verified/corrected the first audit's deferred RISKY items, §B found ~26
new findings, including two genuinely live bugs: `.github/workflows/termwiz.yml`
permanently red on `ubuntu-latest` since termwiz is now unconditionally
Windows-only, and `docs/install/source.md`/an issue template falsely claiming
Strawberry Perl is needed "for building openssl" when openssl isn't even in the
Windows dependency graph).

User then explicitly asked for a `/babygoal` round using **`/crush` AND git
worktrees** (parallel isolation, not sequential like the prior round). Decomposed
into 8 tasks (#537-544): six independent-by-file tasks (A-F) launched
**simultaneously**, each in its own `git worktree` + its own `crush run --session`,
then a 3-task sequential chain (G→H, chained after F) for the two/three tasks that
need `cargo build` verification, serialized deliberately because `CARGO_TARGET_DIR`
is a **shared global env var** across every worktree on this machine — concurrent
builds from different worktrees race on the same target dir (empirically hit this:
task #542's independent verification build failed once with "Access is denied" on
`strip-ansi-escapes.exe` while task #540's build was running concurrently; retried
serially and it passed clean — this was a build-artifact race, not a live
onlyterm-gui.exe process, confirmed by investigation before concluding that).

**Tasks #537-543 are done, verified, and merged into `main`** (each via `git merge
--no-ff <branch>` after independent zero-trust verification — never trusted crush's
self-reported "build succeeded"/"done" claims at face value). This zero-trust
posture caught **real problems in nearly every crush output**:
- #539 (CONTRIBUTING.md/PRIVACY.md): crush wrote relative doc links (`../install/
  source.md`, `config/reference/config/check_for_updates.md`) that were wrong
  because both files live at repo root, not inside `docs/` — fixed to
  `docs/install/source.md` / `docs/config/reference/config/check_for_updates.md`.
- #537 (CI templates): stale "OpenGL initialized!" log placeholder left in
  `bug.yml` despite this fork having removed OpenGL/EGL/WGL entirely; missing
  trailing newlines. Fixed.
- #538 (generate-workflows.py/generate-docs.py): crush's own verification run
  left `ci/__pycache__/*.pyc` **committed** (`.gitignore`'s `/__pycache__/` entry
  is root-anchored, doesn't match nested paths) — untracked the .pyc files and
  widened the gitignore pattern. Also independently re-verified crush's claim
  that Perl is genuinely unneeded (searched `crates/` for real `\bperl\b`
  references — none found, matches crush's own investigation).
- #540 (cosmetic sweep): crush left runs of **blank lines** instead of removing
  lines outright in `.gitignore`/`wgpu-hal-vendored/Cargo.toml`/`deny.toml` —
  squashed them. More seriously, it removed the stale pre-crates/-move
  `.gitignore` entries for `wezterm-char-props/codegen` and `termwiz/codegen`
  build artifacts **without adding the crates/-prefixed equivalents** — verified
  both codegen sub-crates still genuinely exist and are excluded from the main
  workspace (so they CAN produce their own `target/`/`Cargo.lock` if built
  standalone) and restored the entries at the correct paths. Also verified via
  `cargo deny check licenses` (installed, available) that removing the `ring`
  license-clarify block didn't introduce a *new* license-check failure (ring
  isn't reachable in the Windows-target dependency graph today; a pre-existing,
  unrelated `Unicode-3.0`/icu4x license failure exists on `main` too, confirmed
  by running the same check there — not caused by this task).
- #541 (`ci/update-derived-files.sh`): crush investigated real usage first (28
  doc files include its output) and correctly chose to **repair**, not retire —
  fixed the `onlyterm` binary name, the nonexistent `assets/shell-completion/`
  output path, the `cargo run --example narrow -p portable-pty` package name
  (correctly identified `portable-pty` as the crate's actual package name, not
  the `-p pty` guess in the task prompt), and actually re-ran the script,
  regenerating 29 doc files with real, sane `onlyterm.EXE --help` output
  (spot-checked one). A `job_kill` tool call in its trace was investigated (given
  the project's HARD CONSTRAINT about never killing onlyterm processes) and
  determined to be crush's own internal background-job control, not an OS-level
  process kill — no `taskkill`/`Stop-Process` pattern found anywhere in its
  session trace.
- #542 (nix crate target-scoping in wezterm-escape-parser): clean, minimal,
  correct on first inspection — only needed a re-run of the verification build
  after the concurrent-build race above.
- #543 (env-bootstrap snap/AppImage removal): clean, minimal, correct on
  inspection — `fixup_snap()`/`fixup_appimage()` and their call sites removed,
  rest of `bootstrap()` (panic hook, logger setup, etc.) untouched.

**Task #544 is currently in flight** (background `crush run`, session
`task-544-config-deprecation`, worktree `../wezterm-wt-oh2-h`, branch
`cleanup/oh2-h-config-deprecation`) — the last and most involved task. Before
delegating, personally investigated `crates/config/src/config.rs` and found the
codebase already has **six**, not two, existing `#[dynamic(deprecated = "...")]`
precedents (four from an earlier FreeType→rustybuzz+swash rasterizer migration:
`font_colr_rasterizer`, `display_pixel_geometry`, `freetype_interpreter_version`,
`freetype_pcf_long_family_names`; plus `kde_window_background_blur` and
`show_update_window`) — all six load-bearing style references handed to crush.
Also personally confirmed a **live bug**: `docs/config/reference/config/
font_antialias.md` and `font_hinting.md` both carry a genuine upstream 2021
deprecation notice, but neither field exists in `config.rs` today — any user
`.ktav` referencing either hard-fails to load right now (all three config-load
sites use `UnknownFieldAction::Deny`). This is included in #544's scope: add both
fields back with `#[dynamic(default, deprecated = "...")]`, matching the
freetype-migration style, picking appropriate enum types from the docs'
documented possible values. #544 also adds `#[dynamic(deprecated)]` to 10 other
dead platform fields (`macos_forward_to_ime_modifier_mask`, `enable_wayland`,
`enable_zwlr_output_manager`, `macos_window_background_blur`,
`wayland_window_background_blur`, `xim_im_name`, `native_macos_fullscreen_mode`,
`macos_fullscreen_extend_behind_notch`, `xcursor_theme`, `xcursor_size`) —
deliberately EXCLUDING `tiling_desktop_environments`, which the second audit
proved has a real Windows consumer (`crates/wezterm-gui/src/termwindow/
resize.rs` compares it against `connection_name`, which literally is `"Windows"`
on this platform) and must NOT be touched.

Separately during this session window: replied to a public GitHub commit comment
on `PHPCraftdream/OnlyTerm-legacy-fork` (a user asked "why not PR to wezterm" —
verified real numbers via `gh api` first: wezterm/wezterm has exactly 300 open
PRs and 1504 open issues today — then posted a reply citing those numbers and
linking the active repo `PHPCraftdream/OnlyTerm`).

The `/babygoal` Stop-hook from this round is still armed: "заведи таски и решай
их с помощью /crush & worktrees" — satisfied only once TaskList reaches 0
pending/in_progress. Currently #544 is the only non-completed task.

## Active goal

Stop-hook condition (verbatim): "заведи таски и решай их с помощью /crush &
worktrees". Not yet satisfied — task #544 still in_progress.

## TaskList

### in_progress
- #544 oh2-H: Deprecation-шимы для мёртвых платформенных полей config.rs (no blockedBy — G resolved)

### recently completed
- #543 oh2-G: Убрать snap/AppImage fixup-код из env-bootstrap
- #542 oh2-F: Убрать nix-крейт из Windows-сборки (target-scope в Cargo.toml)
- #541 oh2-E: Починить или упразднить ci/update-derived-files.sh
- #540 oh2-D: Мелкая косметическая зачистка (осиротевшие файлы/записи)
- #539 oh2-C: Вычистить CONTRIBUTING.md и PRIVACY.md от мёртвых секций
- #538 oh2-B: Починить ci/generate-workflows.py, generate-docs.py и openssl/Perl-дезинформацию
- #537 oh2-A: Починить CI/community-шаблоны под Windows-only форк
- (earlier round, not itemized here) #532-536: first oh-audit cleanup round

## Decisions

- Chose parallel-sub-agents-with-worktree-isolation strategy for this round per
  the user's explicit request ("с помощью /crush & worktrees"), vs. the strictly
  sequential strategy used in the prior round — required serializing only the
  build-verifying tail (F→G→H) to avoid the shared `CARGO_TARGET_DIR` race, while
  letting the six non-conflicting, mostly-non-build tasks (A-E, F) run truly
  concurrently.
- Continued the zero-trust verification posture established earlier this
  session: never accepted a crush "done"/"build succeeded" claim without an
  independent re-check. This is not precautionary boilerplate — it caught real,
  distinct defects in 4 of the first 6 tasks (broken relative links, an
  accidentally-committed `__pycache__`, a silently-dropped `.gitignore`
  entry-class, and — pending final confirmation — will apply the same rigor to
  #544's font_antialias/font_hinting fix once it lands).
- Investigated and pre-briefed crush with the *correct* existing deprecation
  precedent (`#[dynamic(deprecated)]`, 6 live examples) before delegating #544,
  rather than letting it discover/guess a mechanism — this was itself found by
  personally reading `config.rs` first, which also surfaced the font_antialias/
  font_hinting live bug that neither audit's original wording had fully pinned
  down (the audit only said "add a shim or fix the docs", this checkpoint's
  investigation determined definitively that adding the shim is correct, and why).
- Determined `job_kill` in task #541's crush trace was crush's own internal job
  control (not an OS-level process kill) by absence-of-evidence (no
  `taskkill`/`Stop-Process` pattern anywhere in its session trace) plus a clean
  git commit/working-tree-clean end state — accepted as sufficiently verified
  rather than blocking further on it, given the HARD CONSTRAINT's actual concern
  (killing live onlyterm-gui.exe user sessions) had no positive evidence of
  happening.

## Open questions

- #544's outcome is unknown as of this checkpoint — still running in background.
  On resume: check `D:/dev/rust/wezterm-wt-oh2-h/.crush/stdin/task-544.out`,
  then independently verify (git show, cargo build/clippy/test/fmt) before
  merging, per the established pattern for every other task this round.
- Whether/when to push `main` to `origin` — not requested since the last
  session's force-push (after the `git filter-repo` history rewrite); `main` is
  now many commits further ahead.
- The second `@oh` audit's remaining, not-yet-actioned findings (deferred as
  lower priority than what became #537-544): B2 (wgpu-hal-vendored `[[example]]`
  sections already handled in #540, but double-check nothing else in that
  Cargo.toml still references deleted paths), the ~56 docs/ files with genuinely
  mixed Windows/non-Windows content needing per-paragraph editing (not
  file-level deletion — explicitly out of scope for a crush-delegated pass),
  and whatever `[graph] targets` scoping fix for `deny.toml` was flagged (B25)
  but not part of this round's scope.

## Repo state

```
 M (none currently uncommitted on main; all landed via merge commits)
?? .claude/
?? docs/checkpoints/2026-08-10-1942.md
?? docs/checkpoints/2026-08-10-2355.md
?? docs/checkpoints/2026-08-11-1554.md
?? docs/checkpoints/2026-08-11-2242.md
```

```
f55dcec14 Merge cleanup/oh2-g-env-bootstrap
6c59c3164 cleanup: remove Linux-only packaging code from env-bootstrap
6b595d7d0 Merge cleanup/oh2-f-nix-target-scope
3cb3536b4 Merge cleanup/oh2-d-cosmetic
6fbc1dae3 fix: restore codegen gitignore entries, clean up blank-line cruft
```

One active worktree remains, intentionally, for the in-flight task:
`D:/dev/rust/wezterm-wt-oh2-h` on branch `cleanup/oh2-h-config-deprecation`
(HEAD at `f55dcec14`, same as `main` — no commits yet, crush still working).
`/babysit` cron `c22ca902` (every 15 min, off-minute schedule) is armed and will
keep nudging this forward / self-delete once TaskList is empty.
