# Checkpoint — 2026-08-11 15:39 [review-round-2-oh-followups]

## Session summary

Direct continuation of the `2026-08-11-1422.md` checkpoint (same session, no compaction). That
checkpoint was written mid-flow, right after tasks #482–489 (the 8 findings from
`docs/investigations/2026-08-11-new-commits-review.md`) were implemented/committed and task #493
(`@oh` review of that round) was still pending.

`@oh`'s review of the #482–489 round (task #493, completed) came back positive overall: no
"no-op" fixes slipped through (it specifically re-checked #485, the one `crush` produced a broken
no-op fix for earlier, and confirmed the reimplementation is genuinely correct), all substantive
fixes hold up, build/clippy/test all clean per crate. It found exactly one blocker
(`cargo fmt --check` — run workspace-wide, not `-p wezterm-gui` as I'd been doing — failed on one
line in `862fab1b2`) and six smaller, non-blocking findings. Fixed the fmt blocker immediately
(`94bb827ea`). Created tasks #495–498 for the six findings (one task per finding cluster, grouped
by file/theme) and #499–501 mirroring the #490–493 wrap-up shape (checkpoint → commit .md →
`@oh` review), all chained via `blockedBy`.

The user's Stop-hook condition (armed via `/babygoal` earlier this session: "реализуй задачи с
помощью /crush ... После завершения ВСЕХ ЗАДАЧ ... checkpoint, changelog, commit, review") kept
firing on subsequent turns since 4 new tasks existed with `pending`/`in_progress` status — the
hook doesn't distinguish "the original scope is done" from "new tasks got created from a review
pass"; it just checks the live TaskList. Correctly continued the loop rather than treating the
original #493 completion as satisfying the condition.

Executed #495–498 in the `blockedBy` chain, same zero-trust pattern as before. This round:
`crush` was attempted only for #495 (lost-wakeup race in `draw.rs`) and stalled with zero changes
(same abrupt-empty-message-after-bash-call failure mode observed repeatedly in the first round) —
given `crush`'s declining hit rate on the smaller/later tasks all session, implemented #495–498
directly myself rather than continuing to retry:

- **#495**: fixed the lost-wakeup race `@oh` found in `call_draw_webgpu`'s backpressure check
  (`crates/wezterm-gui/src/termwindow/render/draw.rs`) — re-check `is_in_flight()` right after
  `set_repaint_pending()` and invalidate the window directly if it flipped to `false` in that
  gap. Needed a `use window::WindowOps;` import fix after the first build attempt (caught by my
  own build, not left in). Committed `7f72f19c3`.
- **#496**: removed the dead `set_gui_hang_callback`/`GUI_HANG_CALLBACK` plumbing and stale
  doc-comment in `crates/window/src/os/windows/watchdog.rs` (orphaned once the toast callback
  registration was removed earlier this session). Committed `9b69b9cfb`.
- **#497**: the more substantive one — `ProcessGpuContext::get_or_init`
  (`crates/wezterm-gui/src/termwindow/webgpu/context.rs`) used a `std::sync::Mutex` that had to
  be released before the async `Self::new(config).await`, letting two concurrent callers race
  into duplicate GPU-device initialization. Switched to `smol::lock::Mutex` (already a direct
  dependency via `smol.workspace`), whose guard *can* be held across an `.await`, so the whole
  check-init-cache sequence is now genuinely serialized — this also let me delete the
  now-unnecessary post-init reconciliation branch and its dead `current_version >= expected_version`
  condition (`@oh`'s finding #6) outright, rather than just fixing it. Needed one fmt fixup
  (a two-line `static` declaration rustfmt wanted collapsed to one line) caught by my own
  `cargo fmt --check`. Committed `6cb4e4b3b`.
- **#498**: corrected two overstatements in the changelog entries added during round 1 (the
  device-lost-subscriber entry overstated what was leaked and understated that pruning still only
  runs on an actual device-lost event, not continuously; the AHash entry overclaimed
  "closing that class of attack" for a non-cryptographic hash) and added the two changelog entries
  that were missing from round 1's scope entirely (the Alt+V dead-key fix, `73ee22826`; the
  watchdog false-positive fix + toast removal, `4a96ad3c2`) plus brief mentions of #495 and #497's
  own fixes for completeness. Not yet committed — that's task #500's job, same split as round 1.

Currently executing #499 (`/checkpoint`, this file). Remaining: #500 (commit the second
checkpoint + `docs/changelog.md`), #501 (`@oh` review of #495–498), in that `blockedBy` order.
`/babysit` cron `4cfe033a` remains armed (15-minute interval, unchanged since round 1).

## Active goal

The Stop-hook condition from the original `/babygoal` invocation remains armed (verbatim: see the
prior checkpoint's summary). It is satisfied once the TaskList reaches 0 pending/in_progress AND
the checkpoint/changelog/commit/`@oh`-review sequence for whatever's currently on the list has
run — which is exactly what #499–501 are for.

## TaskList

### in_progress
- #499 Финальный /checkpoint после завершения задач #495-498

### pending
- #500 Закоммитить .md артефакты второго раунда (checkpoint/changelog) (blockedBy: #499)
- #501 Ревью @oh: проверить работу по задачам #495-498 (blockedBy: #500)

### recently completed
- #498 Поправить неточности в docs/changelog.md + добавить записи про вотчдог/Alt+V
- #497 context.rs: вернуть сериализацию инициализации GPU-контекста + мелкие правки
- #496 Убрать мёртвый код device-lost hang-callback в watchdog.rs
- #495 Потерянное пробуждение в backpressure-проверке call_draw_webgpu
- #493 Ревью @oh: проверить всю работу по задачам #482-489 (round 1)
- #482–#489, #490–#492, #494 (round 1 implementation + wrap-up + the urgent Alt+V report) — see
  `2026-08-11-1422.md` for that history.

## Decisions

- Treated the Stop-hook's repeated "insufficient progress" feedback as the literal, standing
  directive it's documented to be (per the system prompt's Stop-hook instructions) — continued
  working through #495–501 without pausing to re-confirm with the user, exactly as the hook's own
  guidance specifies.
- Stopped retrying `crush` for the smaller round-2 tasks after one more stall (#495) — given the
  session-wide pattern (crush's hit rate declined sharply on later/smaller tasks: solid on
  #482–484/487, unreliable-to-broken on #485/486/488/489, and now #495), implemented #496–498
  directly rather than spending further turns on a tool showing a consistent failure signature.
- For #497, chose `smol::lock::Mutex` (already a direct dependency) over adding a new crate or a
  hand-rolled "in-progress" state machine — the minimal-diff option that directly enables holding
  the lock across the `.await`, which is the actual property needed to eliminate the race.
- For #498, went beyond just fixing the two flagged overstatements: also added changelog entries
  for #495/#497 themselves and for the two earlier-session fixes (Alt+V, watchdog) that had never
  been documented at all, since the task's purpose (docs matching reality) extends naturally to
  "don't leave shipped, user-facing fixes undocumented" even though strictly the review only
  flagged the two overstatements.

## Open questions

- None new. Same as the prior checkpoint: whether/when to push (still not requested this
  session), and the `crush` bash-call-stall reliability issue (now reinforced by a 6th
  occurrence) is worth surfacing to the user as a standalone observation once this round closes
  out, separate from the task work itself.

## Repo state

```
 M docs/changelog.md
?? .claude/
?? ci/dev-install.log
?? docs/checkpoints/2026-08-10-1942.md
?? docs/checkpoints/2026-08-10-2355.md
```
(docs/checkpoints/2026-08-11-1539.md, this file, and the docs/changelog.md changes are staged for
task #500, not yet committed.)

```
6cb4e4b3b gui: serialize WebGPU context init instead of racing concurrent callers
9b69b9cfb window: remove dead device-lost-hang-callback plumbing from the watchdog
7f72f19c3 gui: fix lost-wakeup race in the WebGPU backpressure check
94bb827ea gui: fix cargo fmt violation in the startup-command warning log
c808c4fca docs: changelog, checkpoint, and investigation doc for the review round
862fab1b2 gui: log --start-conf startup-command send failures instead of dropping them
50643a14e gui: mark two benchmark tests #[ignore] so they don't slow cargo test
6e8373d2a gui: use ahash::RandomState instead of fixed-key AHasher for glyph caches
```

`main` is 5 commits ahead of the `2026-08-11-1422.md` checkpoint's HEAD, 15 ahead of the last
pushed tag (`v0.0.7-alpha` at `4a96ad3c2`). Nothing pushed this window.
