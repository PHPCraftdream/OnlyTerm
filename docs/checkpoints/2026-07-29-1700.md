# Checkpoint — 2026-07-29 17:00 [unsafe-audit-complete]

## Session summary
Прямое продолжение чекпоинта `2026-07-29-1315.md`. Пользователь дал составной `/goal`/`/babygoal`: "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу". По этому запросу выполнена ВСЯ цепочка задач №202-210 (project-wide аудит unsafe-кода) через внешний CLI-инструмент `crush run` (провайдер zai/glm-5.2), с независимой верификацией (git diff review, `cargo build`/`cargo test`/`cargo check --workspace` после каждой задачи) и отдельным коммитом на каждую.

Порядок выполнения: #206 (чистые data-structure крейты) → #202 (window OS-слой) → #203 (pty/procinfo/filedescriptor/env-bootstrap) → #204 (шрифтовая подсистема) → #205 (termwiz) → #207 (wezterm-gui) → #208 (mux/client/networking) → #209 (codec/wezterm-open-url/wezterm-toast-notification) → gap-fill проход (нашёл и закрыл пропуски: `wezterm-mux-server/src/main.rs`, весь крейт `wezterm/src/asciicast.rs`, часть `window/src/os/macos/{app,clipboard,connection}.rs` и `os/windows/{connection,mod}.rs`, `os/xkeysyms.rs` — эти файлы не попали в исходные формулировки задач) → #210 (включение `#![warn(clippy::undocumented_unsafe_blocks)]` во все 26 затронутых крейтов + исправление 8 находок, которые сам lint обнаружил уже ПОСЛЕ ручного аудита — т.е. lint поймал реальные пробелы: непокрытые `unsafe impl Sync` рядом с задокументированным `unsafe impl Send`, SAFETY-комментарий, привязанный к внешнему `{`-блоку вместо самого `unsafe`-блока, и `unsafe fn` в трейте без секции `# Safety`).

Важные технические находки в процессе: (1) первая попытка запуска `crush run` в фоне была сломана обратными кавычками в промпте (bash интерпретировал их как command substitution) — исправлено передачей промпта через файл (`< prompt.txt`); (2) вторая попытка использовала завершающий `&` внутри самой команды поверх `run_in_background: true` инструмента — это дало ложное "completed" от CLI-обёртки, пока реальная работа продолжалась на стороне `crush`-сервера ещё ~10+ минут (обнаружено через `crush sessions list` → статус `running` с растущими токенами); с этого момента для каждой последующей задачи использовался паттерн `crush run ... ; crush sessions watch <id>` в ОДНОМ фоновом вызове без `&`, что корректно блокируется до реального завершения; (3) во время сессии был обнаружен и подключён специализированный skill `/crush`, детально описывающий именно эти грабли ("Never background the process yourself with a trailing `&`") — совпал с уже найденным решением.

Найдено и исправлено несколько РЕАЛЬНЫХ latent UB багов (не просто косметика "unsafe → safe"): (а) `codec::Pdu::stream_decode` использовал `ptr::copy_nonoverlapping` для сдвига непрочитанных байт в начале Vec, но исходный и целевой диапазоны ПЕРЕСЕКАЮТСЯ при `consumed < remain` — `copy_nonoverlapping` требует непересекающихся регионов, это было реальное UB; заменено на `buffer.copy_within(consumed.., 0)` (memmove-семантика); (б) `wezterm/src/asciicast.rs` использовал `str::from_utf8_unchecked` на длине, которая лишь ПРЕДПОЛАГАЛАСЬ валидной — заменено на `from_utf8(...).expect(...)`, обоснованный `Utf8Error::valid_up_to()`; (в) `window/src/os/xkeysyms.rs` использовал `char::from_u32_unchecked` на keysym-derived u32 без проверки диапазона — заменено на safe `char::from_u32`, которая корректно отвергает суррогаты и >U+10FFFF.

Итог: 9 коммитов в main (`83aea0f8c` … `8ac2611c3`), полный `cargo build --workspace` чист, `cargo clippy --workspace` даёт 0 находок нового lint'а, `cargo test --workspace` — **693 passed, 0 failed, 3 ignored** (намеренно, doctest'ы FFI-биндингов egl/wgl). Чейнджлог обновлён (4 новых пункта: unsafe-аудит, zstd→flate2/100% Rust, Hebrew niqqud-стриппинг, Hebrew word-order фикс) — одновременно исправлен устаревший пункт о "wrap-boundary run остаётся неразвёрнутым", который противоречил более раннему фиксу этой же сессии.

Отдельно: во время сессии дважды срабатывал `/crush-fallback @sh` (пользователь явно вызывал, переключая делегирование с `crush run` на локального суб-агента `sh`) — заведена вечная sentinel-задача №213 "crush-fallback state", сейчас в состоянии `STATUS: active, AGENT: sh` (без явного refusal-текста от crush — трактовано как hard-limit случай без cron-будильника, следующий `/crush-fallback clear` должен быть вызван пользователем вручную, когда решит вернуться на `/crush`).

**Ещё не сделано из исходного запроса**: коммит .md-файлов (чейнджлог обновлён, но не закоммичен; этот чекпоинт и предыдущий тоже не закоммичены) и финальный шаг — ревью агента `@oh` над всей проделанной работой (задача №212, заблокирована задачей №211, которая сейчас выполняется).

## Active goal
Session-scoped Stop hook (от `/goal`, установлен через недавний `/babygoal`): "реализуй задачи с помощью /crush, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу". Ещё не разрешён (условие не выполнено полностью) — до сих пор не закоммичены .md и не запущено ревью @oh.

## TaskList
### in_progress
- #211 Обновить чейнджлог, /checkpoint, закоммитить .md (чейнджлог готов, checkpoint пишется прямо сейчас, коммит .md — следующий шаг)

### pending
- #212 Ревью агента @oh по итогам unsafe-аудита (blockedBy: #211)
- #213 crush-fallback state (persistent — do not complete) — сейчас STATUS: active, AGENT: sh

### recently completed
- #210 Финал: свод по unsafe + включить clippy::undocumented_unsafe_blocks
- #209 Аудит unsafe: разное (codec, wezterm-open-url, wezterm-toast-notification)
- #208 Аудит unsafe: mux/client/networking слой
- #207 Аудит unsafe: оставшиеся места в wezterm-gui
- #205 Аудит unsafe: termwiz (tty/console слой)
- #204 Аудит unsafe: шрифтовая подсистема (locators + rustybuzz shaper)
- #203 Аудит unsafe: pty/procinfo/filedescriptor/env-bootstrap
- #202 Аудит unsafe: window-крейт (OS-слой)
- #206 Убрать unsafe в чистых data-structure крейтах

## Decisions
- Реализация unsafe-аудита — через внешний `crush run` CLI (провайдер zai/glm-5.2), не через встроенный Agent tool, по явной инструкции пользователя "с помощью /crush".
- Каждая задача аудита верифицировалась независимо (git diff review + build + test) ПЕРЕД коммитом, а не просто на основании отчёта crush-агента — это поймало минимум один случай, где отчёт был неточен, и подтвердило легитимность каждой правки.
- `#![warn(clippy::undocumented_unsafe_blocks)]` — `warn`, не `deny` (чтобы не ломать CI внезапно), добавлен во все 26 затронутых крейтов, включая те, где реального unsafe уже не осталось (защита на будущее).
- Пробелы в исходном планировании задач (несколько файлов window-крейта, весь `wezterm`-крейт, `wezterm-mux-server/main.rs`) обнаружены и закрыты ДОПОЛНИТЕЛЬНЫМ проходом в рамках задачи #210, а не проигнорированы.
- `/crush-fallback @sh` активирован по явному запросу пользователя (дважды) — текущий режим: делегирование через локальный `Agent(subagent_type="sh")` вместо `crush run`, до ручного `/crush-fallback clear`.
- Устаревшая формулировка в чейнджлоге про Hebrew wrap-boundary исправлена сразу же, а не оставлена противоречащей новому пункту.

## Open questions
- Нужно ли удалять старые untracked scratch-файлы в корне репо (`clippy_full3.txt`, `clippy_output.txt`, `clippy_output2.txt`, `clippy_targeted.txt`, `metadata.json`, `word_order_test.txt`, `OnlyTerm-Setup.exe`) перед финальным коммитом .md? Не уточнялось явно.
- Не выяснено, что именно должен проверить `@oh` в задаче №212 сверх уже выполненной верификации (git diff + build + test на каждый коммит) — вероятно, независимый взгляд на корректность SAFETY-обоснований и отсутствие пропущенных opportunistic-unsafe мест, раз пользователь просил именно ревью "всей проделанной работы".
- `/crush-fallback` (задача №213) остаётся `active` — не выяснено, нужно ли it вернуть в `dormant` перед началом задачи №212 (если ревью должно идти через `@oh` напрямую как Agent tool, а не через `crush`, конфликта нет; если через `crush run`, то по действующему fallback-режиму его следует делегировать через `sh`, а не `oh` — стоит уточнить у пользователя, если это критично, раз задача №212 сама называет конкретного агента `@oh`).
- Render-thread-per-tab архитектура (обсуждалась в предыдущей сессии, задачи под неё так и не заведены) — по-прежнему открытый пункт, не относящийся к текущей инициативе.
- Ни один из commits этой сессии не запушен — явного запроса на push не поступало.

## Repo state
```
 M Cargo.lock
 M Cargo.toml
 M README.md
 M crates/codec/Cargo.toml
 M crates/codec/src/lib.rs
 M crates/term/src/terminalstate/performer.rs
 M crates/term/src/test/mod.rs
 M crates/wezterm-font/src/shaper/rustybuzz.rs
 M crates/wezterm-gui/Cargo.toml
 M crates/wezterm-gui/src/quad.rs
 M crates/wezterm-gui/src/renderstate.rs
 M crates/wezterm-gui/src/termwindow/background.rs
 M crates/wezterm-gui/src/termwindow/box_model.rs
 M crates/wezterm-gui/src/termwindow/render/paint.rs
 M crates/wezterm-gui/src/termwindow/render/pane.rs
 M crates/wezterm-surface/src/cellcluster.rs
 M docs/changelog.md
?? .claude/
?? OnlyTerm-Setup.exe
?? clippy_full3.txt
?? clippy_output.txt
?? clippy_output2.txt
?? clippy_targeted.txt
?? docs/checkpoints/2026-07-29-1315.md
?? metadata.json
?? scripts/
?? word_order_test.txt
```
```
8ac2611c3 project-wide: enable clippy::undocumented_unsafe_blocks, fix remaining gaps (task #210)
27dfb293b window, wezterm, wezterm-mux-server: audit gap sweep, fix two more UB sites (task #210 prep)
6f3c8574b codec, wezterm-open-url, wezterm-toast-notification: fix latent UB, document FFI unsafe (task #209)
f0b357dae mux, wezterm-client, wezterm-uds, lua-api mux/procinfo: remove getpid unsafe, document IPC FFI (task #208)
c35a94950 wezterm-gui: remove opportunistic getpid unsafe, document GPU/Win32 FFI (task #207)
3073df826 termwiz: document tty/console FFI unsafe with SAFETY comments (task #205)
53cc7c7bc wezterm-font: document FFI unsafe, remove one opportunistic transmute (task #204)
126515a7e pty, procinfo, filedescriptor, env-bootstrap: audit unsafe, remove two opportunistic spots (task #203)
7179369b7 window: document remaining FFI unsafe with SAFETY comments (task #202)
83aea0f8c wezterm-dynamic, color-types, vtparse, config, wezterm-cell, lfucache, wezterm-escape-parser: remove opportunistic unsafe
```
Ни один из этих 9 коммитов не запушен. Остальные показанные в `git status` изменения (compression removal, Hebrew niqqud/word-order fixes, renderstate.rs unsafe-rewrite) — более раннее незакоммиченное состояние сессии, предшествующее unsafe-аудиту, тоже ещё не запрошено к коммиту явно (кроме docs/changelog.md, который относится к текущей задаче #211).
