# Checkpoint — 2026-07-31 08:45 [tab-isolation initiative done; final @oh review in flight]

## Session summary

Direct continuation of checkpoint `2026-07-31-0815.md`, written moments ago. Since that checkpoint, the final step of the governing `/babygoal` instruction ("после завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh") has been carried out in full: `docs/changelog.md` was updated with a full set of user-facing bullets covering the completed tab-isolation initiative (in-place WebGpu renderer rebuild superseding the old "close the window" behavior, pane-level hang isolation, the documented headless-mux-server topology, the pty buffer/socketpair changes, the Windows socketpair TOCTOU fix, and the `run_child_process` timeout), both checkpoint files (`2026-07-30-1330.md` and `2026-07-31-0815.md`) were committed alongside the changelog in commit `4899ea9bd`, and a comprehensive final review was dispatched to an `@oh`-class agent covering the full commit range `3fb93fd0e..HEAD` (23 commits: everything from the pty-BUFSIZE reduction through the changelog/checkpoint commit). That review agent was launched and is running in the background (async, not yet returned) at the moment this checkpoint was requested -- its result has NOT yet been seen or acted upon. The user invoked `/checkpoint` directly, interrupting only the local turn (not the background review agent, which continues running independently and will still deliver a completion notification when done).

TaskList is now essentially empty of actionable work: every task from #239 through #264 is `completed` except two deliberate exceptions -- #213 (a persistent sentinel task, "crush-fallback state", never meant to complete) and two explicit "do not start" notes (#235, deferred pending future measurement; #264, a note about `spawn-funcs`'s rhai wiring gap found incidentally, out of scope for this initiative, belongs to a separate lua->rhai migration effort).

The bulk of this session's second half (since the last full context compaction) was a single large initiative: making a hung/wedged tab or window unable to take down anything else. Two rounds of `@oh` research reshaped the plan significantly along the way (per-tab GPU rendering isolation was proven architecturally impossible by citing `wgpu-core`'s internal locking; per-window message-loop and per-tab-process splits were both investigated and deliberately rejected as disproportionate). What shipped instead: full pane-level hang isolation (lock-free signals, bounded-timeout accessors with last-known-good fallback, a non-blocking pty writer, backgrounded process-list refresh, a per-tab frame-build time budget) plus, as the single highest-value piece, an in-place WebGpu renderer rebuild (new device/surface/render-thread via a dedicated child HWND) that lets a window recover from a GPU hang/surface-error/device-lost event without losing any of its tabs, backed by a circuit breaker and an OpenGL-backend runtime fallback. This was independently verified on real GPU hardware three separate times (matching results each time: 3 successful in-place rebuilds, then correct circuit-breaker/fallback engagement, zero panics, zero orphaned processes).

Every commit in this range was independently re-verified by the session orchestrator (not just trusted from the implementing `@sh` sub-agent's own report) via build+test+clippy in git-stash isolation from the rest of the (long-standing, unrelated, still-uncommitted) working-tree changes -- this discipline caught one real problem this session: commit `5445746b0`'s `Cargo.lock` accidentally absorbed an unrelated, still-uncommitted `codec` crate dependency change (zstd->flate2) that had nothing to do with the task at hand; fixed immediately in a dedicated follow-up commit (`5518fa4ff`).

A brief detour mid-session: at the user's request the delegation mechanism was switched from the `Agent` tool (`@sh`/`@oh`) to the external `crush run` CLI for one task; both attempts either timed out or exited without completing the intended commit (one left an orphaned `git stash` with partially-finished work, which was found and cleanly merged back in without losing progress). The user then re-invoked `/babygoal` with explicit "с помощью @sh" wording, and the session reverted to the `Agent` tool for everything since.

## Active goal

`/babygoal` cycle: "реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh - пусть проверит всю работу." Every step of this is now done or in flight: tasks complete, checkpoint written (this file), changelog updated, all `.md` changes committed (`4899ea9bd`) -- the ONLY remaining piece is the final `@oh` review, which is running in the background right now and has not yet returned a result. Once it returns, the orchestrator still needs to: read its findings, fix anything it flags as real (following the same independent-verification discipline as every other commit this session), and only then can this governing instruction be considered fully satisfied.

## TaskList

### pending (persistent / explicitly deferred, not real open work)
- #213 crush-fallback state (persistent sentinel — do not complete)
- #235 (221.9, deferred pending measurement — do NOT start without explicit user decision)
- #264 (note: `spawn-funcs::register_rhai` not wired into the live GUI's rhai engine — out of scope for this initiative, belongs to a separate lua→rhai migration; do NOT start without explicit user decision)

### recently completed (this part of the session, in rough chronological order)
- #239-#241: pty reader buffer size, TCP-loopback→in-process channel, Windows socketpair TOCTOU fix
- #242, #244-#251: full pane-level (non-GPU) hang isolation chain
- #252-#255: WebGpu child HWND, in-place renderer rebuild (the initiative's make-or-break step), device-lost/surface-error coverage, OpenGL runtime fallback
- #256-#257: end-to-end wedged-pane regression test, documented real-GPU manual verification
- #224 (reopened/reverified): documented the headless-mux-server multi-process topology as the practical answer to "window = separate process"
- #258, #259: closed as deliberate no-ops after final `@oh` scoping (per-window message loop and per-tab process both rejected as disproportionate)
- #260-#262: flaky-test survey (found nothing beyond the one known test) and fix (self-calibrating ratio instead of an absolute threshold)
- #263: `run_child_process` timeout (the actual, cheaper replacement for what #258/#259 would have addressed)

## Decisions
- Per-tab GPU rendering isolation: proven architecturally impossible (not just expensive) via `wgpu-core`'s internal `snatchable_lock`/`fence` semantics — never revisit without a different GPU abstraction layer.
- Per-window message loop (#258) and per-tab separate process (#259): investigated and explicitly rejected — disproportionate engineering cost (weeks; ~190 spawn sites + 207 `Mux::get()` sites needing re-audit) against a narrow, already-cheaply-fixed residual risk (`run_child_process`'s missing timeout, now fixed in #263).
- Zero-trust verification discipline (independent git-stash-isolated build+test+clippy for every commit, real manual GPU-hardware testing for rendering changes) remains mandatory without exception — it caught a real bug this session (the `Cargo.lock` contamination in `5445746b0`).
- Delegation reverted from `crush run` back to the `Agent` tool (`@sh`) after two failed `crush` attempts, per explicit user instruction.

## Open questions
- The final `@oh` review of commit range `3fb93fd0e..HEAD` is in flight (background, async) and has not yet returned. Its findings still need to be read, triaged, and (for anything real) fixed before this session's governing `/babygoal` instruction is fully satisfied.
- Whether/when to ever commit the long-standing, unrelated, still-uncommitted working-tree changes (codec zstd→flate2, term/performer.rs and onlyterm-font/shaper Hebrew fixes) remains open, inherited from every prior checkpoint this session — not touched, not part of this initiative.
- No commit from this session has been pushed.
- Task #264 (spawn-funcs/rhai wiring gap) — whether to ever turn this into real work is the user's call; it belongs to a separate, already-referenced lua→rhai migration effort, not this initiative.

## Repo state
```
 M Cargo.lock
 M Cargo.toml
 M crates/codec/Cargo.toml
 M crates/codec/src/lib.rs
 M crates/term/src/terminalstate/performer.rs
 M crates/term/src/test/mod.rs
 M crates/onlyterm-font/src/shaper/rustybuzz.rs
?? .claude/
?? OnlyTerm-Setup.exe
?? clippy_full3.txt
?? clippy_output.txt
?? clippy_output2.txt
?? clippy_targeted.txt
?? metadata.json
?? scripts/
?? touched.tmp
```
```
4899ea9bd docs: update changelog and checkpoint the tab-isolation initiative
af4eab85e spawn-funcs: bound run_child_process by a timeout (task #263)
7feb4999b mux: make chunking hold-time test self-calibrate vs. its own baseline (task #261)
32578085a docs: document surviving a killed GUI process via headless mux-server (task #224)
34ea1d715 docs: manual real-GPU verification of in-place renderer rebuild (task #257)
```
