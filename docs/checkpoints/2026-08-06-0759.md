# Checkpoint — 2026-08-06 07:59 [opengl-removal-complete-plus-font-round]

## Session summary

Continuation of a long multi-day OnlyTerm (Windows-focused onlyterm fork) session, driven by `/babygoal` + `/babysit` (cron job `eac6d1f1`, `7,22,37,52 * * * *`, 15-min interval, still armed). This window completed the entire OpenGL-removal thread and a parallel font/config bug-fixing round, using sequential `@sh` background sub-agents, each independently re-verified by me (build/clippy -D warnings/fmt/test rerun myself before ever trusting a sub-agent's self-report or committing its work) before being accepted.

**OpenGL removal (task chain #412-417) — fully complete.** #412 (survey/map) and #413 (remove `FrontEndSelection::OpenGL`/`Software` from config) were already done before this window. This window: #414 (remove the GL/glium renderer from `onlyterm-gui` — `renderstate.rs`, `uniforms.rs`, GLSL shaders, the WebGpu-fallback subsystem in `render_pipeline.rs`; replaced with a clean-fail path via `terminate_with_error_message`) — commit `056e4b7a2`. #415 (remove GL/EGL/WGL context code from the `window` crate across all platforms — `wgl.rs`, `egl/`, `configuration.rs`'s `prefer_swrast` stub, GL-specific parts of x11/wayland/macos window code) — commit `2f77e1134`; deleted `glium`/`wayland-egl`/`cgl` from `Cargo.toml`/`Cargo.lock` as a side effect, which satisfied #416 with no separate work needed. #417 (reassess task #372 now that GL is gone) became moot: the user directly deleted task #372 themselves mid-session ("давай удалим таску - Падение при возврате на WebGPU..."), bypassing the "needs separate authorization" gate by their own explicit choice.

**Font/config round (tasks #420, #421, #422, #426) — fully complete, each independently verified.** #420: audited all OS-dependent paths in `config`; concluded existing per-OS defaults (`font_locator`, `dirs_next`-based home/XDG resolution, `default_prog`) were already correct, and that `font_dirs` already resolves relative entries safely against the config file's directory — the only real gap was documentation, so a doc-only fix (new `docs/config/reference/config/font_dirs.md` portability section + a regression test) was chosen over adding new placeholder syntax, deliberately rejecting a `$SYSTEM_FONTS`-style mechanism because it would steer users toward the exact "point font_dirs at a whole system fonts directory" pattern that caused task #422's crash — commit `2709e7cd4`. #421: added a targeted hint to the "No fonts contain glyphs" warning (log + toast) for the `font_dirs` set / `search_font_dirs_for_fallback` false misconfiguration from task #419, plus a new `docs/config/reference/config/search_font_dirs_for_fallback.md` page — commit `b0a853ab7`. #422 (critical stack-overflow investigation): the original "unbounded recursion in COLR/COLRv1 paint-graph" hypothesis was **disproven** empirically (isolated repro, bisected to a single trigger file, every font-parsing call succeeds even on a 64KiB stack); the real trigger turned out to be the *first* call to `Config::default()` in the process — `FromDynamic`'s derive macro generates one large ~256-512KiB-stack non-recursive function body for the ~226-field `Config` struct, comfortably under Windows' 1MiB default main-thread stack on its own, but happened to be reachable from `ParsedFont::from_face`'s `ignore_svg_fonts` check exactly when a font file was being parsed, misdirecting the original investigation. No production code was changed (correctly, per instructions not to guess a fix without a confirmed root cause); two regression-canary tests were added instead. Honest caveat preserved in the task record: re-running the exact original crashing config against a fresh build did **not** reproduce the crash, possibly because this session's own OpenGL removal shifted the startup call-stack depth — this is a plausible but unconfirmed explanation. Commit `de89fe8b5`. The user's live mitigation (`font_locator: Gdi`, no `font_dirs`) remains the operative protection regardless. #426: root-caused and fixed a real bug — `GdiFontLocator::attributes_to_descriptor` hardcoded `stretch: FontStretch::Normal` in the DirectWrite descriptor regardless of what was actually requested; `dwrote`'s exact-match-only lookup then always failed for any family whose real stretch isn't Normal (e.g. Lucida Console = SemiCondensed), silently forcing every resolution onto the slower legacy GDI path. Fixed by threading the real requested stretch through. Honest caveat: this is a confirmed, legitimate bug, but the user's *exact* trigger scenario (system DPI-scale change + terminal zoom) could not be reproduced in isolation — the live-DPI-change half of the repro was never tested directly. Commit `16594b6e3`. **Notable incident during #426**: the first dispatched background agent (`ae694c0dd1664fbad`) went dark for the better part of 7 hours (no commits, no file changes, no cargo/rustc activity for extended stretches) during `/babysit` ticks. Diagnosed as a genuine stall (not routine polling — checked `tasklist`/file mtimes only when evidence warranted it), sent two escalating `SendMessage` nudges (first one did trigger renewed activity; the process eventually produced the same commit `16594b6e3`, confirming it was alive throughout, just extremely slow), and in parallel launched a fresh resume agent (`a404e6684492b9379`) after the second nudge went unanswered for 30+ minutes, which picked up the original agent's still-uncommitted-but-correct diff and finished the job properly within its own budget. Both agents ultimately converged on the identical commit hash — no duplicate/conflicting work resulted, but this is worth remembering as a real example of the "long silent gap that resolves itself vs. genuinely dead" ambiguity `/babysit` has to navigate.

**A separate, unrelated fix landed mid-session**: task #407 (white/transparent rectangles when a second OnlyTerm window is in the background) — while investigating fresh user reports that it was *not* actually fixed by task #425 (a prior session's placeholder-teardown-timing fix), found and fixed a real, distinct root cause in `crates/onlyterm-gui/src/termwindow/webgpu/state_impl.rs`: the WebGpu swapchain was configured with `CompositeAlphaMode::PostMultiplied`/`PreMultiplied` (DWM alpha-blend compositing) even when the user never configured any window transparency, combined with every frame clearing the surface to alpha 0 before drawing — any transient mismatch between the opaque background quad's bounds and the actual swapchain size (e.g. mid-resize) left a literal transparent gap that DWM composited with whatever window was behind it. Fixed by defaulting to `CompositeAlphaMode::Opaque` unless real transparency is configured (`window_background_opacity != 1.0` or a background image/gradient/layer) — commit `6984af94a`. **This fix is intentionally NOT yet in the changelog** — it's scoped to task #409 (blocked behind the #399-401 review-fix chain, which is itself blocked on #407 needing a user retest before closing), to avoid documenting it twice. The startup-flicker half of the user's #407 report (separate from the transparency half) remains unaddressed and unconfirmed either way.

**Final round wrap-up (task #418), in progress right now.** Updated `docs/changelog.md`'s "Continuous/Nightly" section: rewrote the stale OpenGL-fallback description (lines ~291-338) to reflect its complete removal and the new fail-clean startup behavior, marked `front_end: OpenGL`/`Software` as **Breaking**, and added new `Fixed`/`New` entries for #420/#421/#424/#426 (deliberately did NOT add entries for #407, #405, #406, #425 — those are #409's scope once the #399-401 chain closes, to avoid duplicate/conflicting documentation of the same fixes across two different final rounds). Just wrote this checkpoint file. Still to do in this same turn: commit `docs/changelog.md` + this checkpoint file + the other still-uncommitted checkpoint (`2026-08-05-2151.md`, `2026-08-05-startup-ux-loader-redesign.md`), then launch an `@oh` review agent over the whole round's commits per the original `/babygoal` instruction, and finally mark task #418 completed.

## Active goal

No `/goal` Stop hook (the session runs on a Stop-hook condition text tied to the original `/babygoal` invocation: "реализуй задачи с помощью @sh, между тасками делай коммиты. После завершения всей работы сделай /checkpoint, обнови чейнджлог, закомить все мд и запусти ревью агента @oh — пусть проверит всю работу" — this checkpoint is itself satisfying part of that condition). `/babysit` cron `eac6d1f1` remains armed at 15-min intervals.

## TaskList

### in_progress
- #418 Финал: changelog + чекпоинт + коммит .md + ревью @oh для этого раунда (#415-417, #420-422, #426) — changelog done, checkpoint just written, still need: commit .md files, launch @oh review, mark complete

### pending
- #369 crush-fallback state (persistent marker — never complete)
- #399 #392: fade обрывается на любом WM_WINDOWPOSCHANGED (blockedBy: #407)
- #400 #391/#394: SAFETY-комментарий про DeleteObject неверен (blockedBy: #399)
- #401 #391/#394: два комментария устарели после Drop for PlaceholderSpinner (blockedBy: #400)
- #402 Window::new_window: двойное освобождение Rc (pre-existing/upstream — do NOT touch without separate explicit authorization)
- #407 Второе окно OnlyTerm в фоне вызывает белые/прозрачные прямоугольники — transparency half FIXED this session (commit `6984af94a`, not yet in changelog, deliberately deferred to #409); flicker half still unconfirmed; awaiting user retest on a fresh build before closing
- #408 СТОП-ПРАВИЛО: никогда не убивать чужие процессы OnlyTerm (persistent standing rule, never complete)
- #409 Финал: changelog для #399-401/#405/#407 (blockedBy: #401)
- #410 Финал: чекпоинт+коммит .md (blockedBy: #409)
- #411 Финал: ревью @oh (blockedBy: #410)

### recently completed
- #426 onlyterm-font: fix hardcoded DirectWrite stretch=Normal (commit `16594b6e3`)
- #422 Investigated stack overflow; disproved font-parsing recursion theory, found real cause (Config::default()'s large derive-generated stack frame), added regression canaries only, no production fix (commit `de89fe8b5`)
- #421 onlyterm-font: hint at search_font_dirs_for_fallback in missing-glyph warning (commit `b0a853ab7`)
- #420 config: document font_dirs OS-portability, no code/placeholder change (commit `2709e7cd4`)
- #415 OpenGL-removal: GL context removed from window crate (commit `2f77e1134`, also satisfied #416's glium-removal via side effect)
- #414 OpenGL-removal: GL renderer removed from onlyterm-gui (commit `056e4b7a2`)
- #413 OpenGL-removal: FrontEndSelection::OpenGL/Software removed (commit `ebf64e85b`, prior session)
- (mid-session, unrelated) #407 transparency root-cause fixed via opaque alpha_mode (commit `6984af94a`) — task itself stays open pending user retest

## Decisions

- OpenGL removal fully complete across config, onlyterm-gui, and the window crate; `glium`/EGL/WGL/Mesa/ANGLE gone entirely from the dependency tree and codebase. WebGpu is the sole backend; startup failure now fails cleanly with a clear error instead of degrading.
- Task #422: do not guess a fix without a confirmed root cause — when the original hypothesis was disproven and the real trigger (Config::default()'s stack frame) posed no live risk under the default 1MiB stack, ship regression canaries instead of touching production code.
- Task #420: prefer documentation over new config syntax (`$SYSTEM_FONTS`-style placeholders) specifically because such a mechanism would steer users toward the exact anti-pattern that caused #422's crash.
- Changelog scope discipline: #418's changelog update covers only #413-417/#420-422/#426; #407/#405/#406/#425 are deliberately left for #409 (a separate, already-scoped final task) to avoid duplicate or conflicting documentation of the same fixes.
- `/babysit`-tick stall handling: diagnose with real signals (active `rustc`/`cargo` processes, file mtimes) before concluding a background agent is dead; when genuinely dark for an extended, escalating period, nudge via `SendMessage` before resuming/relaunching, and treat a relaunch converging on the same commit as the original agent as confirmation it was never actually duplicated, just slow.

## Open questions

- Task #407: transparency half fixed (commit `6984af94a`), but the user has not yet retested on a fresh build; the startup-flicker half of their report is still unaddressed and its relationship to the already-shipped #425 fix remains unclear.
- Task #422: is the "OpenGL removal shifted the startup call-stack depth enough that the original crash no longer reproduces" explanation actually correct? Never verified directly (would require reproducing on a pre-OpenGL-removal binary for comparison, not attempted).
- Task #426: the user's literal reported trigger (system DPI-scale change followed by terminal zoom) was never reproduced in isolation — only the terminal-zoom half was tested. If the warning recurs, the agent's suggested next lead is Windows `FontSubstitutes` registry entries or a corrupted/duplicate Lucida Console registration causing GDI to silently substitute a different face.

## Repo state

```
 M docs/changelog.md
?? .claude/
?? ci/dev-install.log
?? docs/checkpoints/2026-08-05-2151.md
?? docs/checkpoints/2026-08-05-startup-ux-loader-redesign.md
```

```
16594b6e3 onlyterm-font: pass the requested stretch through to DirectWrite instead of hardcoding Normal (task #426)
de89fe8b5 onlyterm-font/config: investigate task #422 stack overflow, add regression canaries (task #422)
b0a853ab7 onlyterm-font: hint at search_font_dirs_for_fallback in the missing-glyph warning (task #421)
2709e7cd4 config: make OS-dependent paths portable across platforms (task #420)
6984af94a webgpu: use an opaque swapchain composite-alpha mode by default (task #407)
```
