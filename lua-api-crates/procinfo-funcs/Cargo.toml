[package]
name = "procinfo-funcs"
version = "0.1.0"
edition = "2021"
publish = false

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
anyhow.workspace = true
config.workspace = true
libc.workspace = true
procinfo.workspace = true
wezterm-dynamic.workspace = true
# rhai port of `register` (L4b, see
# docs/plans/2026-07-23-lua-rhai-migration.md): `src/lib.rs::register_rhai`
# registers rhai-side equivalents of every function `register(lua: &Lua)`
# registers, in parallel with the mlua path.
rhai.workspace = true
